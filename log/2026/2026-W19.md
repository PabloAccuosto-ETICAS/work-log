# Week 2026-W19 (May 04 – May 10, 2026)

> First entry of this work log. Bootstrapped on Monday May 04, 2026
> (W19 day 1) capturing the substantial operational work done today.

## Career Scoops audit

Operational work on the project repo (`Eticas-AI/career-scoops-audit`).
No client-facing activity; the project remains in a stable resting
state.

- **Repo bootstrap and restructure.** Migrated the Dec 2025 flat
  layout (inherited from Melissa Robles) into a structured
  repository: `code/`, `data/`, `docs/` with RISE-semantic
  subdirectories (`R-reflection`, `I-individualized-reports`,
  `S-strategic-insights`, `E-exposure`). Added `.gitignore`. Removed
  legacy notebook duplicates after content-comparison verification.
- **Project knowledge consolidation.** Moved the carry-forward
  context document (`career-scoops-context.md`) and the audit
  retrospective (`career-scoops-retrospective-v3.md`) from Claude
  project knowledge into the repo. Established the rule that the
  repo is the single source of truth for project state and that
  Claude project knowledge stays intentionally empty of state.
- **Deliverables consolidation.** Wrote per-deliverable summaries
  for the seven Gates Foundation deliverables under
  `docs/deliverables/summaries/` (one per deliverable, ~50k chars
  total). Established the SharePoint location as the canonical
  store for the binaries; the in-repo summaries are the pre-digested
  operational context for routine work.
- **Project framing correction.** Mid-session correction: the
  Career Scoops project is the audit *broadly* understood (not just
  the technical inspection of the system but the full evaluation of
  the procurement and infrastructure ecosystem around it). All
  seven deliverables are products of this single engagement, not
  "siblings" to it. Codified in `STATE.md` "Decisions in force" to
  prevent regression.
- **Pattern alignment with `Eticas-AI/ai-ops`.** Filled the gaps
  the canonical pattern requires (root README, TRACKER.md,
  `data/README.md` and `data/MANIFEST.md`). Documented the local
  deviations (six intentional choices) in `meta/README.md`.
- **Folder rename.** When `ai-ops` rewrote the canonical pattern
  around the Claude/GitHub contract and renamed the operational
  folder from `docs/audit-state/` to `meta/`, applied the rename
  here. Four files moved + four old files deleted in clean commits.
  Old paths in older log entries were left as written (the log is
  append-with-corrections).
- **Microsoft 365 MCP connector verified.** SharePoint is now
  directly readable. Codified the "summary-first" rule: routine
  reads use the in-repo summaries, direct SharePoint reads are
  reserved for cases where a summary is insufficient. This avoids
  creating habits that depend on a connector that may not be
  enabled in every conversation.

For the full chronological detail, see
[`Eticas-AI/career-scoops-audit/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/career-scoops-audit/blob/main/meta/INTERNAL_LOG.md).

## Wiselook audit

Operational alignment work; no audit-content activity this week.
The audit itself remains blocked on partner API credentials from
Wiselook (requested 27 April, reminded 29 April).

- **Repo bootstrap.** Created `docs/audit-state/{STATE.md,
  INTERNAL_LOG.md, README.md}` and moved the internal log out of
  Claude project knowledge into the repo. Updated the root README
  as a pointer to STATE. Reset Claude memory entries to the two
  stable workflow conventions. Opened the migration to the canonical
  pattern.
- **Pattern alignment.** Migrated `docs/audit-state/` → `meta/`
  applying the canonical pattern revision in `Eticas-AI/ai-ops`.
  Four files moved + three old files deleted in clean commits.
  Project instructions updated in the Claude UI to point to the new
  paths and the reissued pattern, including the new drift-check
  bullet (a small process improvement that came out of the dual-
  session work this week, where a parallel session in Career Scoops
  applied an older version of the pattern before the canonical
  refactor landed).
- **Counterparty artefacts indexed.** Added `docs/counterparty/README.md`
  with an in-repo index of legal/reference artefacts (NDA, MSA, SOW,
  architecture PDF), pointing to SharePoint (canonical) and project
  knowledge (operational copy). The artefacts themselves stay where
  they are — this codifies the three-location pattern explicitly so
  future sessions know what exists and where.

For the full chronological detail, see
[`Eticas-AI/wiselook-audit/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/INTERNAL_LOG.md).

## Risk taxonomy

Post-release operational work following v0.3.0 (released last week). The taxonomy adopted the Claude project pattern this week, and several inputs came in for the next release.

- **Documentation refresh PR.** Refreshed README, TAXONOMY, CHANGELOG (full v0.3.0 entry), TRACKER (Phase 5 completion stats by framework), `docs/external-framework-alignment.md` (full rewrite for the 9-category × 10-framework alignment with three-bucket grouping), and four other docs files (intro-for-team, editing-guide, implementation, add-category/subcategory consistency). The verification doc preamble was also updated from "draft, pending validation" to "applied".
- **Internal nav fix.** Fixed hardcoded `/risk/` links in `_layouts/concept.html` that broke navigation when browsing `/risk-internal/`. The fix uses Liquid conditional `{% if page.url contains '/risk-internal/' %}` to switch the base path. Also bumped `src/config.yaml` from `version: "0.1.0"` to `"0.3.0"` (was rendering "Version 0.1.0" on the index pages).
- **Per-mapping citations and source URLs documented as pending workstream.** Originally surfaced during the v0.3 mapping work (research already in `docs/external-framework-mapping-verification.md`). Documented in `TRACKER.md` as a candidate for v0.3.x or v0.4. Likely partially superseded by the validation review (next bullet).
- **Validation review and new framework candidates queued for next release.** A stress-test of the existing ~75 category-level mappings was incorporated as input for the next release: 8 priority + 6 recommended corrections identified across three issue types (imprecise concept names vs framework terminology, match-types too strong/weak, mappings where cited articles don't address the claimed risk). A proposal to add TC260 (China, type: reference) and Council of Europe CETS 225 (compliance, category-level) was incorporated alongside. Both pending external review before application. Source documents in SharePoint canonical.
- **Claude project pattern adoption.** Migrated to a dedicated Claude project on the Eticas Team account following `Eticas-AI/ai-ops/claude-project-pattern.md`. PK kept empty (Career Scoops shape) — repo is sole source of truth. State, context, and chronological work consolidated into `meta/`. Inventory entry in `Eticas-AI/ai-ops/inventory.md` updated.
- **Cloudflare migration plan documented.** Plan for adding access control to the internal site (`/risk-internal/`) documented in `meta/CONTEXT.md`. Selected approach: repo private + GitHub Actions deploys outputs to Cloudflare Pages + Cloudflare Access on `/risk-internal/`. Zero recurring cost. Targeted as a temporary bridge until the new Eticas website migration. Setup scheduled for next week.

For the full chronological detail, see
[`Eticas-AI/ai-risk-taxonomy/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-risk-taxonomy/blob/main/meta/INTERNAL_LOG.md).

## Transversal / Cross-project

- **Canonical pattern stabilised.**
  `Eticas-AI/ai-ops/claude-project-pattern.md` was rewritten around
  the Claude/GitHub contract and the operational folder name was
  standardised as `meta/`. Both Career Scoops and Wiselook were
  aligned on the same day. The cross-project handoff document used
  to coordinate the changes worked well as a mechanism — worth
  reusing.
- **Microsoft 365 MCP connector setup.** Enabled the M365 connector
  at the user account level and on the Career Scoops project.
  Verified read access to SharePoint sites and OneDrive personal.
  Six tools available, all read-only:
  `sharepoint_search`, `sharepoint_folder_search`,
  `outlook_email_search`, `outlook_calendar_search`,
  `chat_message_search`, `read_resource`. No write tools exposed.
- **Project knowledge policy clarified.** Claude project knowledge
  is intentionally empty of project state across all Eticas
  projects. State lives in the project repos; transversal content
  lives in `Eticas-AI/ai-ops` and other dedicated repos. Memory is
  reserved for stable workflow conventions, not operational state.
- **Memory entries written.** Two stable Claude memory entries
  configured (workflow conventions for Eticas repos + per-contributor
  preferences for solo work and direct recommendations).
- **Work log repo bootstrapped.** This repo (`work-log`) was created
  today (Monday May 04) under `PabloAccuosto-ETICAS/`. Decided to
  live in the personal account rather than `Eticas-AI/` because it's
  a personal artifact even though its content is largely Eticas
  work. Verified the Claude GitHub MCP integration has write access
  to this account.

## Meetings & comms

- **Reply to Noelia clarifying "top four" categories.** Noelia asked which were the "top four" categories referenced in the v0.3 context (where there are 9 categories). Most likely interpretation: the four core categories from earlier versions of the taxonomy (Bias & Fairness, Privacy & Confidentiality, Reliability, Governance) historically used as the audit baseline. Confirmed via Teams.
- **Reply to Gemma on Teams about access control on the internal taxonomy site.** Gemma flagged that the internal site (`risk-internal/`) had no access control. Reply explained that GitHub Pages on a private repo isn't available with a free account; Team plan would allow it but the published site stays public on the internet (so doesn't solve access control); Enterprise solves it but is expensive (~$105/month for 5 people). Proposed Cloudflare Pages + Cloudflare Access as zero-cost temporary solution; full plan in the taxonomy project's `meta/CONTEXT.md`. Setup scheduled for next week.
- **Email to Usman and Gemma summarizing v0.3 implementation.** Structured first-person summary sent after v0.3 was merged: structural changes applied, framework mappings refreshed (316 new entries, 10 frameworks total, AIRO/VAIR retired, AIUC-1/AIR/IBM added), public/internal split implemented, mappings grouped by Compliance / Reference / Taxonomies. Listed pending items.

## Reading & research

(No specific entries this week so far — add as the week progresses.)

---

## Pending items rolling into / through this week

These are concrete actions Pablo identified as still to-do. Items
that complete should get a "→ done <date>" annotation in this entry
rather than be deleted.

- **Career Scoops project instructions update.** Replace the project
  instructions in the Claude UI with the version that points to
  `meta/STATE.md` and `meta/CONTEXT.md` and includes the M365
  source-of-truth bullet and the work-log bullet. → Done Monday
  May 04 (work-log bullet added).
- **Career Scoops project knowledge cleanup.** Delete the 9 legacy
  files from the project knowledge UI (the source documents that
  are now consolidated into the repo).
- **Wiselook integration with work-log.** Apply the same Personal
  customisation bullet to Wiselook's project instructions. Populate
  this week's Wiselook section once the parallel session captures
  what was worked on. → Section populated Monday May 04. Personal
  customisation bullet applied Monday May 04.
- **Wiselook project knowledge cleanup.** A previous-session list of
  legacy / transversal / duplicate files identified for deletion is
  still pending. Independent of the work-log integration.
- **Activate M365 MCP connector in the Wiselook Claude project.**
  Already connected at the account level (verified by
  `search_mcp_registry`), but not active in this specific project.
  Activating it would let future sessions resolve SharePoint deep
  links and read counterparty artefacts directly when needed. Not
  blocking.
- **Apply `meta/` folder and instructions block to `ai-risk-taxonomy`.** Merge the `meta/STATE.md`, `meta/CONTEXT.md`, `meta/INTERNAL_LOG.md` generated during the migration session. Update the inventory entry in `Eticas-AI/ai-ops/inventory.md`.
- **Create the new Claude project for risk taxonomy on the Eticas Team account.** Paste the instructions block from the migration session. Keep PK empty. Validate with a session-start prompt; verify Claude enters via `meta/STATE.md` in the order specified by the instructions.
- **Cloudflare migration setup for `ai-risk-taxonomy` (~2 hours, scheduled for next week).** Make repo private; configure GitHub Actions to deploy outputs to Cloudflare Pages; configure Cloudflare Access on the `/risk-internal/` path with login.
