# Week 2026-W19 (May 04 – May 10, 2026)

> First entry of this work log. Bootstrapped on Monday May 04, 2026
> (W19 day 1) capturing the substantial operational work done today.

## Career Scoops audit

Operational work on the project repo (`Eticas-AI/career-scoops-audit`).
No client-facing activity; the project remains in a stable resting
state.

- **Repo bootstrap and restructure.** Migrated the Dec 2025 flat
  layout (inherited from Melissa Robles) into a structured
  repository: `code/`, `data/`, `docs/` with RISE-semantic
  subdirectories (`R-reflection`, `I-individualized-reports`,
  `S-strategic-insights`, `E-exposure`). Added `.gitignore`. Removed
  legacy notebook duplicates after content-comparison verification.
- **Project knowledge consolidation.** Moved the carry-forward
  context document (`career-scoops-context.md`) and the audit
  retrospective (`career-scoops-retrospective-v3.md`) from Claude
  project knowledge into the repo. Established the rule that the
  repo is the single source of truth for project state and that
  Claude project knowledge stays intentionally empty of state.
- **Deliverables consolidation.** Wrote per-deliverable summaries
  for the seven Gates Foundation deliverables under
  `docs/deliverables/summaries/` (one per deliverable, ~50k chars
  total). Established the SharePoint location as the canonical
  store for the binaries; the in-repo summaries are the pre-digested
  operational context for routine work.
- **Project framing correction.** Mid-session correction: the
  Career Scoops project is the audit *broadly* understood (not just
  the technical inspection of the system but the full evaluation of
  the procurement and infrastructure ecosystem around it). All
  seven deliverables are products of this single engagement, not
  "siblings" to it. Codified in `STATE.md` "Decisions in force" to
  prevent regression.
- **Pattern alignment with `Eticas-AI/ai-ops`.** Filled the gaps
  the canonical pattern requires (root README, TRACKER.md,
  `data/README.md` and `data/MANIFEST.md`). Documented the local
  deviations (six intentional choices) in `meta/README.md`.
- **Folder rename.** When `ai-ops` rewrote the canonical pattern
  around the Claude/GitHub contract and renamed the operational
  folder from `docs/audit-state/` to `meta/`, applied the rename
  here. Four files moved + four old files deleted in clean commits.
  Old paths in older log entries were left as written (the log is
  append-with-corrections).
- **Microsoft 365 MCP connector verified.** SharePoint is now
  directly readable. Codified the "summary-first" rule: routine
  reads use the in-repo summaries, direct SharePoint reads are
  reserved for cases where a summary is insufficient. This avoids
  creating habits that depend on a connector that may not be
  enabled in every conversation.

- **`docs/audit-state/` → `meta/` reference cleanup (Wed May 06).**
  Stale references to the old folder name in `docs/deliverables/README.md`
  (1 reference) and `TRACKER.md` (6 references) updated to `meta/`.
  Two direct commits to main (commits `16c14cc` and `8aa7780`).
  The rename itself is correctly recorded as deviation #7 in
  `meta/README.md` (kept as historical context). Repo is now
  consistent.

For the full chronological detail, see
[`Eticas-AI/career-scoops-audit/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/career-scoops-audit/blob/main/meta/INTERNAL_LOG.md).

## Wiselook audit

Operational alignment work plus one methodological input from
Noelia (the Warden review, see Wed May 06 below). The audit itself
remains blocked on partner API credentials from Wiselook (requested
27 April, reminded 29 April).

- **Repo bootstrap.** Created `docs/audit-state/{STATE.md,
  INTERNAL_LOG.md, README.md}` and moved the internal log out of
  Claude project knowledge into the repo. Updated the root README
  as a pointer to STATE. Reset Claude memory entries to the two
  stable workflow conventions. Opened the migration to the canonical
  pattern.
- **Pattern alignment.** Migrated `docs/audit-state/` → `meta/`
  applying the canonical pattern revision in `Eticas-AI/ai-ops`.
  Four files moved + three old files deleted in clean commits.
  Project instructions updated in the Claude UI to point to the new
  paths and the reissued pattern, including the new drift-check
  bullet (a small process improvement that came out of the dual-
  session work this week, where a parallel session in Career Scoops
  applied an older version of the pattern before the canonical
  refactor landed).
- **Counterparty artefacts indexed.** Added `docs/counterparty/README.md`
  with an in-repo index of legal/reference artefacts (NDA, MSA, SOW,
  architecture PDF), pointing to SharePoint (canonical) and project
  knowledge (operational copy). The artefacts themselves stay where
  they are — this codifies the three-location pattern explicitly so
  future sessions know what exists and where.
- **Methodological review of the Warden AI / Juicebox public bias
  audit (Wed May 06).** Noelia shared the report as an HR-AI
  assurance reference. Surfaced one structural limitation worth
  internalising for our reporting: the "group bias" test binarises
  match scores at the sample median and applies the 4/5ths rule,
  which is structurally insensitive to moderate group bias when
  scores are continuous and Ns balanced. The reported impact ratios
  of 0.95–1.00 across protected attributes are consistent with that
  test geometry, not with evidence of "no bias detected". The
  counterfactual analysis is more informative; its 95% threshold
  is generous. Logged as Key Finding #16 in the Wiselook
  INTERNAL_LOG. Confirms our reporting plan: raw 1–5 scores +
  non-parametric tests + ≥0.5 practical-significance threshold,
  framed as answering different questions, not as "better data".
  Drafted a note to Noelia summarising the four observations.
- **Partner API credentials received (7 May); infrastructure reconnaissance
and driver development, currently blocked on API key validation. (Fri May 08).**
- Received partner API credentials from Roberto (PROD + STG, via Gmail
  confidential mode). Resolves the main blocker since 27 April.
- Infrastructure probes against PROD and STG (Pablo executing locally,
  Claude compute environment down due to Anthropic incident). Key
  findings: STG URL has a typo in Roberto's email (`api-stg` should be
  `api.stg`); OpenAPI spec public without auth at `/api-docs/swagger.json`
  (v0.10.0, 165 paths, STG and PROD byte-identical); STG `/health`
  returns `environment: "production"` (likely hardcoded).
- v1 vs v2 analysis from the OpenAPI spec: v2 is exclusively the
  user-portal surface (22 ops), no scoring/rubric/evaluation changes.
  Removes one question from Wiselook's queue.
- Built partner API client (`tests/wiselook/partner_api.py`) and smoke
  test (`partner_smoke_test.py`). Smoke currently failing: HTTP 401
  "Invalid API key" — key in `.env` verified clean (48 chars, ASCII,
  correct header format per spec). Pending verification against
  Roberto's email (confidential mode blocks copy-paste; OCR or
  "Show original" needed).
- Methodological revisit: 29 April candidate responses unsuitable as
  factorial seeds (test-passing mode, not authentic). Investigated the
  29 April "AI detector" observation; an initial reframe as the
  Completeness Agent was made and later corrected (see entry below).
- **AI-content filter finding corrected (Sat May 09).** The 8 May
  reframe of the 29 April "AI detector" observation as more likely the
  Completeness Agent was wrong. Recollection re-verified: Claire
  responded to one of the 29 April test inputs with an explicit
  AI-generated rejection — i.e. the system runs an AI-content filter
  distinct from the Completeness Agent. Exact wording was not captured
  at the time and is flagged as the first action item for the next
  session. INTERNAL_LOG entry, Key Finding #19, and Next Steps
  corrected; STATE.md TL;DR and "Can move now" updated. Direct commit
  to `main` (`841a56a`).
For the full chronological detail, see
[`Eticas-AI/wiselook-audit/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/INTERNAL_LOG.md).

## Risk taxonomy

Post-release operational work following v0.3.0 (released last week). The taxonomy adopted the Claude project pattern this week, and several inputs came in for the next release.

- **Documentation refresh PR.** Refreshed README, TAXONOMY, CHANGELOG (full v0.3.0 entry), TRACKER (Phase 5 completion stats by framework), `docs/external-framework-alignment.md` (full rewrite for the 9-category × 10-framework alignment with three-bucket grouping), and four other docs files (intro-for-team, editing-guide, implementation, add-category/subcategory consistency). The verification doc preamble was also updated from "draft, pending validation" to "applied".
- **Internal nav fix.** Fixed hardcoded `/risk/` links in `_layouts/concept.html` that broke navigation when browsing `/risk-internal/`. The fix uses Liquid conditional `{% if page.url contains '/risk-internal/' %}` to switch the base path. Also bumped `src/config.yaml` from `version: "0.1.0"` to `"0.3.0"` (was rendering "Version 0.1.0" on the index pages).
- **Per-mapping citations and source URLs documented as pending workstream.** Originally surfaced during the v0.3 mapping work (research already in `docs/external-framework-mapping-verification.md`). Documented in `TRACKER.md` as a candidate for v0.3.x or v0.4. Likely partially superseded by the validation review (next bullet).
- **Validation review and new framework candidates queued for next release.** A stress-test of the existing ~75 category-level mappings was incorporated as input for the next release: 8 priority + 6 recommended corrections identified across three issue types (imprecise concept names vs framework terminology, match-types too strong/weak, mappings where cited articles don't address the claimed risk). A proposal to add TC260 (China, type: reference) and Council of Europe CETS 225 (compliance, category-level) was incorporated alongside. Both pending external review before application. Source documents in SharePoint canonical.
- **Claude project pattern adoption.** Migrated to a dedicated Claude project on the Eticas Team account following `Eticas-AI/ai-ops/claude-project-pattern.md`. PK kept empty (Career Scoops shape) — repo is sole source of truth. State, context, and chronological work consolidated into `meta/`. Inventory entry in `Eticas-AI/ai-ops/inventory.md` updated.
- **Cloudflare migration plan documented.** Plan for adding access control to the internal site (`/risk-internal/`) documented in `meta/CONTEXT.md`. Selected approach: repo private + GitHub Actions deploys outputs to Cloudflare Pages + Cloudflare Access on `/risk-internal/`. Zero recurring cost. Targeted as a temporary bridge until the new Eticas website migration. Setup scheduled for next week.
- **Cloudflare migration completed (Tue May 05).** Site moved off GitHub Pages onto Cloudflare Pages with custom domain `taxonomy.eticas.ai`, deployed via GitHub Actions on push to main. PR #12 (migration) and PR #13 (post-deploy navigation fix: dropped `.md`/`.html` extensions from generated links — Cloudflare Pages doesn't auto-route `.md` the way GitHub Pages did). Repo made private; old GitHub Pages site retired. Zero recurring cost ($0/month). One outstanding piece: Cloudflare Access on `/risk-internal/*` — paused mid-flow because the Microsoft Entra ID setup needs Global Administrator consent in the Eticas M365 tenant and warrants its own focused session. `/risk-internal/` is publicly accessible in the meantime; risk is low because content distinguishes maturity (emerging vs established), not confidentiality.

- **Cloudflare Access on `/risk-internal/*` configured (Wed May 06).** Cloudflare migration now fully closed. Login flow goes Microsoft Entra ID → policy `emails ending in @eticas.ai` → site. Two assumptions from the previous session were revised in passing: (1) Global Administrator consent in the Eticas M365 tenant was *not* required — the five Microsoft Graph delegated permissions used (`email`, `openid`, `profile`, `offline_access`, `User.Read`) are all "Admin consent required: No" and user-consent on first login was sufficient; (2) Cloudflare Access was assumed to require the hostname's DNS zone to live on Cloudflare, which is what the official docs say. In practice, Cloudflare Access accepted `taxonomy.eticas.ai` via the "Switch to custom input" hostname field even though the `eticas.ai` zone is on Namecheap, because the hostname is already configured as a Pages custom domain. This is the path used here. The behaviour is undocumented and could change; deployment.md notes a fallback (Pages Function Basic Auth middleware) if it ever stops working. The DNS migration that would otherwise have been needed is therefore deferred indefinitely.

- **v1.0.0 released (Thu May 07).** First public/paper version. PR #14 merged, commit `720aa81`. Validation review corrections (8 priority + 6 recommended), TC260 selectively added (4 categories, type: reference) and Council of Europe CETS 225 (8 categories at category level only, type: compliance), per-mapping `citation` field at category level. Rendering restructured: compliance + reference unified as "Standards & frameworks"; "Concept" column renamed to "Reference"; internal view gains a Citation column when populated. MIT subdomain labels verified against canonical source — one ID fix (`behavioural-manipulation` 4.3 → 4.2), one Oxford-comma fix (`synthetic-media-abuse`). Version bumped 0.3.0 → 1.0.0 in `src/config.yaml`. The 1.0 jump (rather than 0.4) was Gemma's request to formalise URI stability for external citation in the forthcoming paper.
- **v1.1.0 released (Thu May 07).** Same day as v1.0.0. PR #15 merged, commit `4aea95c`. Adds **Agentic AI** as a new top-level risk category per Usman's same-day proposal — captures risks distinct from standard LLM risks because the agent takes real-world actions, operates across time, and interacts with external systems and other agents. Structure: 1 category, 2 sub-groups (Autonomous Actions and Tool Use; Multi-agent Integrity), 7 subcategories. Counts after both releases: 9 → 10 categories, 18 → 20 sub-groups, 70 → 77 active subcategories. All new entries shipped `maturity: emerging` and *without* external framework mappings — methodology team owns the mapping pass against IBM Atlas, NIST RMF + 600-1, EU AI Act (incl. the new Agents standard Gemma circulated), MIT V4, AIUC-1, AIR 2024, and DPV. Two scope decisions recorded as deliberate gaps on the category note: adversarial threats specific to agents (memory poisoning, etc.) and loss-of-meaningful-human-control over agents — both deferred to future iterations. Acknowledged overlap with `autonomy-system-autonomy` (multi-step actions, tool use, emergent behaviour) recorded as a follow-up; reconciliation deferred to a future minor.

- **GitHub Releases for v1.0.0 and v1.1.0 created (Fri May 08).** Closed the carry-over from Thu May 07: tags `v1.0.0` (on `720aa81`) and `v1.1.0` (on `4aea95c`) verified annotated and pointing to the correct commits; Releases created on top of each tag using the corresponding `CHANGELOG.md` sections as release notes. Both Releases are now live ([v1.0.0](https://github.com/Eticas-AI/ai-risk-taxonomy/releases/tag/v1.0.0), [v1.1.0](https://github.com/Eticas-AI/ai-risk-taxonomy/releases/tag/v1.1.0)). The operation required a fine-grained PAT because the GitHub MCP connector does not expose tag or release write endpoints — see the Transversal section below for the protocol that came out of this.
- **v1.2.0 released (Fri May 08).** PR [#16](https://github.com/Eticas-AI/ai-risk-taxonomy/pull/16) merged, commit `fef3435`. Tag and Release also created same day. Applies Alex's framework mapping additions to the Agentic AI category (received May 8 in `Agentic_AI_Risks_Alex_additions.docx`) and promotes the entire branch from `maturity: emerging` to `established`, making it visible on the public site for the first time. Two new reference frameworks added: **CSA Agentic Profile (NIST AI RMF v1)** and **OWASP Top 10 for Agentic Applications 2026**, both `type: reference`. Three category-level `closeMatch` mappings on `agentic-ai`: CSA Agentic Profile (AG-MP.3 + AG-MP.2 + AG-GV.2), OWASP Agentic Top 10 (ASI01 + ASI02 + ASI03), IBM AI Risk Atlas (Explainability + Robustness + Computational Inefficiency). Promotion decision was Usman's: "we move Agentic to public but keep Autonomy and Agency internal for now" — the asymmetry is acceptable as a temporary state, reconciliation between the two categories remains deferred. Same PR also added rendering of the existing framework `note` field as a "Notes on these frameworks" footnote section under each mappings table on internal pages — the field was already populated for every framework since v0.3 but was not surfaced anywhere in the rendered output. Counts after release: 10 categories / 20 sub-groups / 77 active subcategories (unchanged); external frameworks 12 → 14; public-site categories 7 → 8.
- **NIST 600-1 completeness section added (Fri May 08).** PR [#17](https://github.com/Eticas-AI/ai-risk-taxonomy/pull/17) merged. Documentation-only follow-up that had been carried over since v1.0.x. New section in `docs/external-framework-alignment.md` walks through the 12 NIST 600-1 risk areas one by one, providing the converse view of the alignment table earlier in the document — instead of "for each Eticas category, which framework areas does it map to", it asks "for each NIST 600-1 risk area, where does it land in Eticas". Coverage breakdown: 7/12 directly covered (Confabulation, Data Privacy, Environmental Impacts, Harmful Bias and Homogenization, Human-AI Configuration, Information Integrity, Information Security), 4/12 partially covered with named gaps (CBRN, Dangerous/Violent/Hateful Content, Obscene/Degrading/Abusive Content, Value Chain and Component Integration), 1/12 true gap (Intellectual Property). Position taken: named gaps are more useful than weak matches for audit defensibility. Surfaced during the work: the broader document still opens with "Eticas AI Risk Taxonomy (v0.3)" and uses v0.3-era headline numbers throughout; v1.0/v1.1/v1.2 changes are not yet reflected. Recorded as a separate pending workstream in `meta/STATE.md` ("Documentation refresh of `docs/external-framework-alignment.md` for v1.0/v1.1/v1.2"); deliberately not bundled with this PR to keep the NIST 600-1 addition reviewable in isolation.

For the full chronological detail, see
[`Eticas-AI/ai-risk-taxonomy/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-risk-taxonomy/blob/main/meta/INTERNAL_LOG.md).

## DataPACT

Operational work on the project repo (`Eticas-AI/datapact`).
DataPACT is the EU Horizon Europe consortium project under Grant
Agreement 101189771 (M1–M36, started Jan 2025). Eticas is partner
#3, leading the ethical & social impact framework (D4.1 / WP4) and
contributing ITACA to the compliance toolbox (WP3). Pablo is the
day-to-day Eticas contributor; Gemma is the Eticas lead.

- **Repo bootstrap and migration to the Eticas Team account.**
  Migrated from a personal-account Claude project to a fresh repo
  following the canonical pattern. Skeleton committed: root
  `README.md` and `TRACKER.md`;
  `meta/{STATE.md, CONTEXT.md, INTERNAL_LOG.md}`;
  `docs/deliverables/` with index README and six summaries (DoA
  Parts A and B, D1.4, D2.1, D3.1, D4.1). PK kept small by design
  (DoA Part B, D4.1 revised, D3.1) — the rest stays in SharePoint
  canonical, accessed via M365 MCP. Inventory entry in
  `Eticas-AI/ai-ops/inventory.md` added.
- **EAB / WP1 / T1.4 consolidation.** Captured the open internal
  thread on EAB restructuring (Gemma/Mariana/Alex emails 24–27
  April), Anja's WP1 status flag, and Eloise's check-in via Ollie.
  Made Eticas's WP1 role explicit in `meta/CONTEXT.md`: T1.4 is
  the only open WP1 task; T1.4 feeds D1.3 (M30, June 2027); added
  an EAB section covering composition (Eloise, Sharon, Jan),
  Eloise's contract terms, and the restructured model under
  discussion. Decisions logged in `TRACKER.md`: preserve 2
  meetings/year cadence per Eloise's contract (renegotiation more
  friction than reward); TK / MUNDAT re-identification risk
  workstream deprioritised after Gemma's call following limited
  progress on the May 04 follow-up meeting.
- **EAB coordination meeting outcomes (Tue May 05 afternoon).**
  Met with Mariana and Alex. Decisions: next EAB will be online
  (not anchored to a consortium meeting); Alex and Mariana will
  prepare a mapping document of the project's technical and
  regulatory components to clarify role allocation for the EAB
  workstream. Several downstream items (D1.3 administrative
  assignment, Pablo's substantive input prompt for the board, the
  written T1.4 update to Anja, second EAB scheduling) now depend
  on the mapping doc output. Logged in `TRACKER.md` as decision
  and action items.

- **`docs/deliverables/README.md` updated to document both SharePoint
  locations (Wed May 06).** Resolved a TBC marker in the README:
  DataPACT has two SharePoint locations with different roles, not
  one. Consortium SharePoint (SINTEF tenant) is canonical for
  delivered deliverables; Eticas SharePoint is operational
  (working drafts, internal admin). Verified during the update
  that the M365 MCP from the Eticas account does *not* access the
  SINTEF tenant — consortium SharePoint is human-fetched (download
  + upload to chat or PK on demand). Added "Which to use when"
  routing guidance. PR
  [Eticas-AI/datapact#1](https://github.com/Eticas-AI/datapact/pull/1)
  merged.

For the full chronological detail, see
[`Eticas-AI/datapact/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/datapact/blob/main/meta/INTERNAL_LOG.md).

## Leaflet

Operational work on the project repo (`Eticas-AI/leaflet`). The
leaflet sits at the end of the audit pipeline (audit process →
audit report → leaflet) and is the user-facing summary delivered
to procurement, regulators, and insurance. Its v4 design was
already documented; this week takes it from design to working
software.

- **SharePoint references cleanup (Wed May 06).** Renamed the
  `docs/references.md` SharePoint section from "SharePoint
  canonical" to "SharePoint antecedents", fixed the directory link
  (was an Outlook host URL, now the proper SharePoint folder URL),
  attributed the pre-Pablo compilation document
  (`OS - AI 'Nutrition Leaflet' Proposal.docx`) to Oliver Smith
  (Feb 2026 leadership meeting), and explicitly framed early
  positions (trust stamp vs. key criteria label, IAAA partnership)
  as superseded — making clear the SharePoint material is historical
  context and no longer actively maintained. Direct commit to
  `main` (`9a10825`).
- **Python generator bootstrap (Wed May 06).** Working implementation
  of the v4 design lands at `src/leaflet/`: Pydantic data contract
  for `AuditReport` (canonical upstream) and `Leaflet` (downstream
  view), R1–R5 scoring engine, Jinja2 LLM renderer, `leaflet render
  | validate` CLI, informational metrics registry, and a synthetic
  Wiselook-0.1 audit-report fixture exercising all five risk
  dimensions (B&F, Reliability, Privacy, Security & Misuse,
  Governance). 41 tests passing end-to-end. The fairness methodology
  stance from the Warden review under Wiselook (raw 1–5 scores +
  non-parametric tests + pre-declared ≥ 0.5-point practical-
  significance threshold, no binarisation, four-fifths rule
  referenced as EEOC legal floor only) is captured in the registry
  and the fixture's `methodology_notes`, and is visible on the
  rendered leaflet itself — making it a deliberate differentiator
  vs. Warden-style assurance for institutional buyers. PR
  [Eticas-AI/leaflet#3](https://github.com/Eticas-AI/leaflet/pull/3)
  open for review (13 commits including bundled STATE.md /
  INTERNAL_LOG.md updates).

- **CLI quality-of-life and aggregation-rule renaming (Thu May 07
  + Fri May 08).** Two related polish passes on the v0.1 implementation.
  (1) `leaflet render` `--out` now defaults to
  `output/<input-stem>.html` (gitignored at repo root), resolving
  the working-tree-clutter issue surfaced by the May 06 local
  verification. (2) The R1–R5 aggregation rule labels were renamed
  to descriptive evidence-pattern names — `critical_and_broad` (E),
  `critical_or_high_majority` (D), `high_or_medium_majority` (C),
  `mostly_clean` (A), `limited_concerns` (B, default) — to remove
  the misleading numeric ordering (engine applies E → D → C → A → B,
  not 1 → 5) and decouple labels from the grade scale, so the rule
  set survives a future change of grades without re-naming.
  Conditions and outputs unchanged; landed across four direct
  commits to `main` (`e3cc414`, `f23060d`, `e64a9bd`, `cf45615`).
  Resolves two open questions (CLI default location, rule-numbering
  vs application order) recorded in `meta/STATE.md`.

- **`examples/` folder + Netlify config in repo (Fri May 08).** PR
  [Eticas-AI/leaflet#4](https://github.com/Eticas-AI/leaflet/pull/4)
  merged. Introduces `examples/<audit-name>/` (with `leaflet.html`
  + `meta.yaml` + `README.md`) as the canonical place for
  pipeline-rendered audit cases, distinguished by `meta.yaml`'s
  `type` (synthetic | real) and `deploy` (true | false) fields —
  per-case visibility control. `scripts/build_site.py` assembles
  `_site/` from `docs/html/` + the deploy-marked `examples/`, run
  by Netlify per a new `netlify.toml` at the repo root that
  replaces the previous UI-only Netlify configuration. First
  example: Wiselook-0.1 synthetic, deployed publicly. Renders are
  produced locally by the contributor running the CLI and committed
  — the build does not re-render, keeping the deploy environment
  free of the leaflet package's dependency surface. One operational
  snag during rollout: Netlify UI's Base directory setting bled
  through the toml on first deploy preview, fixed by explicit
  `base = "."` in `[build]`.

- **`docs/html/templates/` renamed to `docs/html/mockups/` (Fri
  May 08).** PR [Eticas-AI/leaflet#5](https://github.com/Eticas-AI/leaflet/pull/5)
  merged. Disambiguates from the Jinja templates at
  `src/leaflet/templates/` and from pipeline-rendered cases at
  `examples/` — the v4 hand-crafted leaflets are *mockups* (design
  previews with fictional data), not templates in any technical
  sense. Files renamed (preserving git history), internal text and
  CSS class updated, six cross-referencing files patched, two 301
  redirect rules in `netlify.toml` keep the old `/templates/...`
  URLs working. Bonus fix surfaced incidentally: three
  `<a href="leaflet-template-*-v4.html">` links in the rationale
  doc had been 404'ing on the live site since the `docs/html/`
  reorganisation of May 06 — broken because the relative paths
  resolved against the rationale's own `/rationale/` directory;
  fixed by switching to `../mockups/leaflet-mockup-*-v4.html`. No
  content updates to v4 mockups themselves; v5 deferred.

- **Rationale v5 begun (Sat May 09).** New
  `docs/html/rationale/leaflet-rationale-v5.html` alongside v4
  (retained as stable reference per the May 7 versioning-flexibility
  decision), incorporating Usman/Gemma review comments on the v4
  SharePoint document. Headline change is a two-signal framing of
  the aggregation rules — *Peak severity* (catastrophic-failure
  flag) + *Breadth of concern* (systemic-degradation flag) —
  preceding the existing rules table; rules and grade outputs
  unchanged, the table now decomposes each rule into its peak and
  breadth conditions explicitly. Other changes: severity bands made
  concrete via the practical-significance margin from the metrics
  registry (replaces "well above / near / below threshold"), new
  run-repetition policy (worst-of-N, document N) for stochastic
  checks, new mandatory audit-coverage / benchmark-saturation field
  on the LLM leaflet (N prompts, benchmark sizes, saturation
  scores, runs per stochastic check), grade scale moved below the
  rules, explicit "A excludes high severity by construction"
  callout. v5 closes with an "Open from v4 review" section listing
  six deferred comments with per-item rationale: Post-Processing →
  post-training rename (conflates lifecycle stage with training
  stage — pending discussion), category-summary authorship
  (methodology-workstream question), LLM-evaluation lit review
  (separate workstream), audit-coverage field wiring into the data
  contract / Jinja template / mockups (implementation pass to
  follow), continuous (mean-based) vs count-based formulation of
  the breadth signal (real-data dependent — overlaps with the
  existing v0.1-conjecture aggregation-rules question already in
  STATE.md), and three SharePoint-only items not applicable to the
  repo. Index page updated to link both v4 and v5, with v5 marked
  WIP. Direct commit to `main` (`eea0d58`). Priority shifts
  captured in the leaflet's STATE.md: ADM-path validation drops in
  priority per Gemma (LLM first, then port to ADM — mostly
  mechanical, fewer prospective clients today exclusively on
  ADMs); risk-taxonomy alignment relaxed (taxonomy at v1.2 in
  `Eticas-AI/ai-risk-taxonomy` but the leaflet does not need full
  alignment now — bounded drift acceptable). Incidental finding
  flagged in INTERNAL_LOG: the SharePoint DOCX uses "mean score"
  wording while the canonical HTML uses majority counts (per
  `scoring.py`); some of Usman's 2026-05-08 comments were anchored
  against the older wording.

- **Rationale v5 revised after post-commit review (Sat May 09).** The
  original v5 (`eea0d58`) was reviewed post-commit in the same session
  and several issues surfaced — corrections substantial enough to
  warrant a revision pass on a feature branch rather than re-pushing
  to `main`. PR [Eticas-AI/leaflet#6](https://github.com/Eticas-AI/leaflet/pull/6)
  merged (`57ab645`). Corrections: comment authorship cleaned up
  (Usman only; the earlier "Usman/Gemma" co-attribution was a
  mistake); breadth-of-concern reformulation re-framed as a deliberate
  design choice (preventing dilution of a single severe finding)
  rather than a v0.1 conjecture; the deferred-items table scoped
  strictly to design questions (the "wire audit-coverage field"
  implementation row was removed — that work belongs in `STATE.md`,
  not the rationale); navigability addressed via a TOC plus
  `<details>`/`<summary>` collapsibles for §4–6, Appendix, and
  Deferred; index page reordered (Rationale → Worked examples →
  Architecture → Design mockups) and the worked-example description
  clarified to emphasise it is pipeline-rendered from a synthetic
  fixture, not a hand-crafted mockup. Operational rules codified in
  `claude-personal-preferences.md` across two commits in the same
  session — initial set in `d23bcc7` (approval gate before GitHub
  writes, no end-of-session assumption from a single completed task,
  PAT-preferred for `Eticas-AI/*`), refined later (`1dc471c`)
  to expand PAT scope to all Pablo's repos under a single token (via
  classic PAT with `repo` scope, after the fine-grained PAT proved
  unable to span owners) and to prefer git CLI over MCP for write
  operations.

- **Repo reframed as dual-purpose: design + package (Sat May 09).** PR
  [Eticas-AI/leaflet#7](https://github.com/Eticas-AI/leaflet/pull/7)
  merged (`837a07d`). The repo originally documented leaflet *design*;
  with the Python package operational on the LLM path since the
  May 06 bootstrap, the public-facing framing had drifted (root
  README still introduced the repo as a design home and mentioned
  the package as a side note in "Current state"). Restructured the
  root README around two explicit functions — design documentation
  and package implementation — with a new "What's in this repo"
  section enumerating both, a "Quickstart" section at the top
  (install + `validate` + `render` + `pytest`), and a "Status"
  section replacing "Current state". Light pass to
  `src/leaflet/README.md` updating taxonomy version (v0.3 → v1.2),
  expanding the rationale link (implements v4; v5 propagation
  pending), and pruning a resolved `[TBC]` (rendered-HTML location,
  settled 2026-05-08). Refreshed `docs/html/index.html` dates:
  subtitle now reads "Last updated: 9 May 2026", and v4/v5
  descriptions are parallelised with concise date markers.
  Two-commit shape preserved (READMEs vs index) so a future revert
  can isolate either change cleanly.

For the full chronological detail, see
[`Eticas-AI/leaflet/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/leaflet/blob/main/meta/INTERNAL_LOG.md).

## Transversal / Cross-project

- **Canonical pattern stabilised.**
  `Eticas-AI/ai-ops/claude-project-pattern.md` was rewritten around
  the Claude/GitHub contract and the operational folder name was
  standardised as `meta/`. Both Career Scoops and Wiselook were
  aligned on the same day. The cross-project handoff document used
  to coordinate the changes worked well as a mechanism — worth
  reusing.
- **Microsoft 365 MCP connector setup.** Enabled the M365 connector
  at the user account level and on the Career Scoops project.
  Verified read access to SharePoint sites and OneDrive personal.
  Six tools available, all read-only:
  `sharepoint_search`, `sharepoint_folder_search`,
  `outlook_email_search`, `outlook_calendar_search`,
  `chat_message_search`, `read_resource`. No write tools exposed.
- **Project knowledge policy clarified.** Claude project knowledge
  is intentionally empty of project state across all Eticas
  projects. State lives in the project repos; transversal content
  lives in `Eticas-AI/ai-ops` and other dedicated repos. Memory is
  reserved for stable workflow conventions, not operational state.
- **Memory entries written.** Two stable Claude memory entries
  configured (workflow conventions for Eticas repos + per-contributor
  preferences for solo work and direct recommendations).
- **Work log repo bootstrapped.** This repo (`work-log`) was created
  today (Monday May 04) under `PabloAccuosto-ETICAS/`. Decided to
  live in the personal account rather than `Eticas-AI/` because it's
  a personal artifact even though its content is largely Eticas
  work. Verified the Claude GitHub MCP integration has write access
  to this account.
- **Migration runbook authored in `Eticas-AI/ai-ops`.** Added
  `migration-runbook.md` to operationalise the canonical pattern's
  §7 checklist. Distils what worked across the Career Scoops,
  Wiselook, and DataPACT migrations into a six-phase playbook with
  explicit boot sequence, three project shapes (empty PK /
  counterparty PK / hybrid), and a "Decisions crystallised" section
  that prevents re-litigating settled items per migration. Two
  same-day refinements after first review added the per-document
  PK decision step, SharePoint URL capture as an explicit step,
  the "no GitHub MCP" alternative path (script-based bootstrap
  for sessions running from accounts without connectors), and a
  hard rule that PK files never go through git. A subsequent
  generalisation pass removed first-person references so the
  runbook reads as a team-usable playbook usable by any Eticas
  contributor. Invocation pattern: paste a one-liner pointing the
  next session to the runbook URL and let it run.
- **Eticas Claude project workflow refactored (Wed May 06).** Six
  merged PRs in `Eticas-AI/ai-ops`, in order:
  - **#1** — Adds §3.1 to `claude-project-pattern.md`: the PK-as-cache
    pattern for large meta files (PK as a working copy that persists
    between sessions, with SHA handshake at session start).
  - **#2** — Introduces `instructions-common.md`, the operational
    counterpart to the pattern doc. Loaded at session start, holds
    the source-of-truth hierarchy, workflow rules, and meta-file
    efficiency rules that previously duplicated across project
    instruction blocks.
  - **#3** — Restructures §5: per-contributor preferences move from
    inlined sections inside each project's block to a separate file
    in the contributor's personal repo. Common section template
    shrinks substantially.
  - **#4** — Two fixes: corrects obsolete "personal-customisation
    section" references in `instructions-common.md` (left over from
    PR #3), and adds a fourth bullet to "Source of truth" for
    project-declared additional sources (e.g., SharePoint via M365
    MCP). Surfaced when migrating DataPACT.
  - **#5** — §3.1 reworked: the PK cache cycle was structurally
    complete but described what happens, not who does what. Now
    Claude runs the cycle automatically — handshake at session start
    (Hit / Miss / Bootstrap), and at session end delivers the
    cached file as a downloadable artefact + STATE.md delta + upload
    instruction, without being asked.
  - **#6** — Inverts the §3.1 default. `meta/INTERNAL_LOG.md` is now
    cached by default in any project with a `meta/` folder containing
    it. Additional cached files and any opt-out are declared in
    `meta/README.md`. No per-project ceremony needed for the canonical
    case.
- **Project instruction blocks migrated to short form (Wed May 06).**
  All four projects (Wiselook, DataPACT, Risk taxonomy, Career
  Scoops) now use the post-PR-#3 short-form block: identification +
  three pointers (instructions-common, personal preferences, entry
  point) + project-specific notes only. Each block carries only what
  genuinely doesn't generalise and doesn't change with project
  progress (engagement framing, contacts, system pointers). Stable
  facts and operational state stay in `meta/STATE.md` per project.
- **Personal preferences file created** at
  `PabloAccuosto-ETICAS/work-log/meta/claude-personal-preferences.md`.
  Captures: solo work by default, direct commits to main for state
  files, direct recommendations over option lists, clarifying
  questions only for genuine forks, end-of-session work-log entries
  (report-grade 1–3 bullets per project, skip if no substance, don't
  duplicate within ISO week).
- **PAT protocol authored in `Eticas-AI/ai-ops` (Fri May 08).**
  Added a "GitHub access: connector and PAT" section to
  `instructions-common.md` and a corresponding §9 to
  `claude-project-pattern.md`, formalising the use of fine-grained
  Personal Access Tokens for operations the GitHub MCP connector
  cannot perform (tags, GitHub Releases, workflow files, large-file
  edits, bulk operations). The protocol covers the default
  behaviour at session start (`ask-on-start`), recognised
  per-contributor overrides (`ask-when-needed`, `never`), how
  Claude handles a PAT in-session (code-block-only paste, env var
  for the session's lifetime, never persisted), where PATs must
  never be stored (project instructions, project knowledge, repo
  files, `meta/`), and recommended fine-grained PAT scopes. A
  later revision the same day reframed the connector/PAT choice
  as three explicit categories (connector-only / PAT-preferred /
  PAT-only) — surfaced from a real-world test that found the
  decision rule was implicit. Failure modes (auth fail, scope
  insufficient, wrong resource owner, org policy block) and a
  smoke-test routine were added as separate subsections. Pablo's
  personal preferences file declares a per-contributor refinement:
  PAT for Eticas-AI repos, MCP-only for personal repos
  (`PabloAccuosto-ETICAS/*`) to avoid juggling two PATs in the
  same session. The protocol was tested in the same session by
  closing the v1.0.0 / v1.1.0 GitHub Releases — see Risk taxonomy
  section above.
- **`Eticas-AI/ai-ops/README.md` rewritten for human readers
  (Fri May 08).** The previous README assumed familiarity with
  the pattern's vocabulary; the rewrite adds a "Why this exists,
  and what you get" section explaining the persistent-context
  problem the pattern solves, in plain language, and a "How to
  get started" section pointing to the social path (talk to
  someone first, then bootstrap from there). Existing structure
  (what lives here / does not live here / audience / maintenance)
  preserved with softer technical language. Aimed at colleagues
  across mixed technical levels who may want to try this on
  their own projects.
- **Pattern made contributor-agnostic + drift check added (Fri May 08).**
  PR [Eticas-AI/ai-ops#13](https://github.com/Eticas-AI/ai-ops/pull/13)
  merged. Two changes to the canonical pattern, surfaced because
  `ai-risk-taxonomy` is the first Eticas Claude project shared by
  more than one contributor: (1) the §5 instructions template is
  now contributor-agnostic by default — step 2 of "read in order"
  states the convention `<contributor>-ETICAS/<repo>/blob/main/meta/claude-personal-preferences.md`
  and instructs Claude to ask which contributor is in the session
  rather than hardcoding a URL; the private form (single hardcoded
  URL) becomes a simplification rather than the default; (2) added
  an active drift-check section to `instructions-common.md` —
  once per session, after reading the project block + common,
  Claude checks for duplication, contradiction, or obsolete shape
  between them. Drift is non-blocking; flagged at start, proposed
  as a delta at session end. The risk-taxonomy project block was
  updated in the Claude UI to the new contributor-agnostic shape
  the same day.
- **PAT handling corrections + git-over-https quirk documented
  (Fri May 08).** PR [Eticas-AI/ai-ops#15](https://github.com/Eticas-AI/ai-ops/pull/15)
  merged. Three corrections to `instructions-common.md` §"GitHub
  access: connector and PAT", surfaced during the v1.2.0 work in
  `ai-risk-taxonomy`: (1) the previous "Handling a PAT" step said
  Claude loads the token into a `bash` env var "for the lifetime
  of the session", which is misleading because each `bash_tool`
  invocation starts a fresh shell — `export GH_TOKEN=…` doesn't
  persist; replaced with explicit file-based persistence
  (`/home/claude/.gh_token` with `chmod 600`, read with `cat` in
  each invocation, `shred -u` at session end). The empty-env-var
  case produces a 401 that looks identical to a revoked-token 401,
  which sent the v1.2 session ~30 minutes down the wrong
  diagnostic path; (2) added "Git over HTTPS auth quirk" subsection
  documenting that `git -c http.extraheader="Authorization: Bearer
  ${GH_TOKEN}"` is documented but observed to fail against
  GitHub's git-over-https endpoint with fine-grained PATs — the
  reliable form is the embedded-credentials URL with
  `x-access-token` as the username, plus a post-clone
  `git remote set-url` to sanitise; (3) strengthened 401 advice:
  before concluding a token is bad, verify it was actually present
  in the call by printing its length (never the value).
- **Issue #14 opened in `Eticas-AI/ai-ops` (Fri May 08).** "Calibrate
  numeric thresholds for MCP-vs-PAT operations". Surfaced during
  v1.2 work: write operations via MCP (especially `push_files`)
  are perceptibly slower than reads because file content sent as
  tool-call argument is paid as tokens generated by Claude,
  scaling linearly with file size. Reads are unaffected (content
  arrives as a result, not an argument). The current
  connector/PAT decision rule in `instructions-common.md` is
  qualitative ("under ~500 lines or so as a rough guide"); the
  issue tracks 2–3 sessions of empirical data collection before a
  PR adds numeric thresholds. Referenced from `ai-ops/meta/STATE.md`
  as a priority workstream.

## Meetings & comms

- **Reply to Noelia clarifying "top four" categories.** Noelia asked which were the "top four" categories referenced in the v0.3 context (where there are 9 categories). Most likely interpretation: the four core categories from earlier versions of the taxonomy (Bias & Fairness, Privacy & Confidentiality, Reliability, Governance) historically used as the audit baseline. Confirmed via Teams.
- **Reply to Gemma on Teams about access control on the internal taxonomy site.** Gemma flagged that the internal site (`risk-internal/`) had no access control. Reply explained that GitHub Pages on a private repo isn't available with a free account; Team plan would allow it but the published site stays public on the internet (so doesn't solve access control); Enterprise solves it but is expensive (~$105/month for 5 people). Proposed Cloudflare Pages + Cloudflare Access as zero-cost temporary solution; full plan in the taxonomy project's `meta/CONTEXT.md`. Setup scheduled for next week.
- **Email to Usman and Gemma summarizing v0.3 implementation.** Structured first-person summary sent after v0.3 was merged: structural changes applied, framework mappings refreshed (316 new entries, 10 frameworks total, AIRO/VAIR retired, AIUC-1/AIR/IBM added), public/internal split implemented, mappings grouped by Compliance / Reference / Taxonomies. Listed pending items.
- **EAB coordination meeting with Mariana and Alex (Tue May 05 afternoon).** Two outcomes: next EAB will be held online (not anchored to a consortium meeting); Alex and Mariana will prepare a mapping document of the project's technical and regulatory components to clarify role allocation for the EAB workstream. Downstream items (D1.3 admin assignment, EAB input prompt, T1.4 update to Anja, second EAB scheduling) now depend on the mapping doc output.

## Reading & research

(No specific entries this week so far — add as the week progresses.)

---

## Pending items rolling into / through this week

These are concrete actions Pablo identified as still to-do. Items
that complete should get a "→ done <date>" annotation in this entry
rather than be deleted.

- **Career Scoops project instructions update.** Replace the project
  instructions in the Claude UI with the version that points to
  `meta/STATE.md` and `meta/CONTEXT.md` and includes the M365
  source-of-truth bullet and the work-log bullet. → Done Monday
  May 04 (work-log bullet added).
- **Career Scoops project knowledge cleanup.** Delete the 9 legacy
  files from the project knowledge UI (the source documents that
  are now consolidated into the repo).
- **Wiselook integration with work-log.** Apply the same Personal
  customisation bullet to Wiselook's project instructions. Populate
  this week's Wiselook section once the parallel session captures
  what was worked on. → Section populated Monday May 04. Personal
  customisation bullet applied Monday May 04.
- **Wiselook project knowledge cleanup.** A previous-session list of
  legacy / transversal / duplicate files identified for deletion is
  still pending. Independent of the work-log integration.
- **Activate M365 MCP connector in the Wiselook Claude project.**
  Already connected at the account level (verified by
  `search_mcp_registry`), but not active in this specific project.
  Activating it would let future sessions resolve SharePoint deep
  links and read counterparty artefacts directly when needed. Not
  blocking.
- **Apply `meta/` folder and instructions block to `ai-risk-taxonomy`.** Merge the `meta/STATE.md`, `meta/CONTEXT.md`, `meta/INTERNAL_LOG.md` generated during the migration session. Update the inventory entry in `Eticas-AI/ai-ops/inventory.md`.
- **Create the new Claude project for risk taxonomy on the Eticas Team account.** Paste the instructions block from the migration session. Keep PK empty. Validate with a session-start prompt; verify Claude enters via `meta/STATE.md` in the order specified by the instructions.
- **Cloudflare migration setup for `ai-risk-taxonomy` (~2 hours, scheduled for next week).** Make repo private; configure GitHub Actions to deploy outputs to Cloudflare Pages; configure Cloudflare Access on the `/risk-internal/` path with login. → Done across two sessions: Pages migration Tue May 05 (PR #12 + post-deploy fix #13); Cloudflare Access on `/risk-internal/*` Wed May 06. Site live at `https://taxonomy.eticas.ai/`, internal site gated by Entra ID SSO. Repo private. Old GitHub Pages site retired.
- **Configure Cloudflare Access on `/risk-internal/*` (carry-over from above).** Microsoft Entra ID app registration + 7 Microsoft Graph delegated permissions + admin consent + Cloudflare Zero Trust identity provider + self-hosted application policy on `taxonomy.eticas.ai/risk-internal/*`. Procedure documented in `Eticas-AI/ai-risk-taxonomy/docs/deployment.md`. Estimated 30–45 min in a focused session; verify Global Administrator role on the Eticas M365 tenant beforehand to avoid mid-flow blocks on admin consent. → Done Wed May 06. Two pre-session assumptions were wrong: (i) Global Administrator role was *not* required — the five delegated permissions used are all "Admin consent required: No"; (ii) the hostname did not need its DNS zone migrated to Cloudflare — the custom-input hostname field accepted `taxonomy.eticas.ai` because it's already a Pages custom domain. Both findings recorded in `meta/INTERNAL_LOG.md` and reflected in the updated `docs/deployment.md`.
- **DataPACT project knowledge cleanup on the Eticas Team account.**
  PK currently has four files; should be three. Remove the duplicate
  D4.1 SUBMITTED (keep only the REVISED). Confirm DoA Part B and
  D3.1 are present.
- **Resolve `[TBC: ...]` markers in DataPACT meta files.** Around
  ten inline markers across `meta/STATE.md`, `meta/CONTEXT.md`,
  `meta/INTERNAL_LOG.md`, and `TRACKER.md` from the bootstrap and
  the EAB consolidation commits. To be resolved in the first
  validation session in the new account.
- **EAB coordination meeting with Mariana and Alex
  (Tue May 05 afternoon).** → Done 2026-05-05. Outcomes captured
  in the DataPACT section above and in the Meetings & comms
  entry of the same date.
- **Mapping document of DataPACT technical and regulatory
  components.** Owner: Alex + Mariana. Due: TBD. Output expected
  to inform D1.3 admin assignment, the substantive input prompt
  for the EAB, and second-EAB-meeting planning.
- **Send written T1.4 status update to Anja.** Anchored on
  outcomes from the mapping doc (post-Tuesday-meeting reframing)
  rather than generic. Closes the silence Anja flagged on Apr 27.
- **Plan second EAB meeting.** First was November 2025; second is
  overdue. Cadence preserved at 2/year per Eloise's contract.
  Format confirmed online; date depends on the mapping doc.
- **Eloise's invoice for the November 2025 EAB meeting.** Eloise
  will submit after the next EAB meeting takes place (her decision,
  confirmed in her Apr 24 email). Cristina to process on receipt.
  Possible RP1 timing question to monitor.
- **Send team announcement about the Claude/GitHub workflow
  (Fri May 08).** Short message in English introducing the
  pattern documented in `Eticas-AI/ai-ops`, framed as an
  invitation to try it on individual projects. Draft prepared
  in the same session that finalised the protocol; to be sent
  end of day along with the weekly summary. Goal: surface the
  option for colleagues across mixed technical levels without
  prescribing adoption.
- **Subcategory-level mappings for Agentic AI** (carried forward
  from v1.2). Alex's May 8 review supplied category-level mappings
  only; subcategory granularity is implied in the citation text
  but not encoded as separate mapping entries. Candidate for v1.2.x
  or v1.3.0 depending on whether the additional YAML surface area
  is judged worth it.
- **Reconciliation between Agentic AI and `autonomy-system-autonomy`**
  (carried forward from v1.1, reaffirmed at v1.2). The two
  categories cover overlapping conceptual territory; current state
  is "Agentic AI public, Autonomy & Agency internal" as a
  temporary asymmetry per Usman + Gemma. Resolution deferred to a
  future minor.
- **Documentation refresh of `docs/external-framework-alignment.md`
  for v1.0/v1.1/v1.2** (surfaced Fri May 08 while adding the NIST
  600-1 completeness section). The document still opens with
  "Eticas AI Risk Taxonomy (v0.3)" and uses v0.3-era headline
  numbers throughout (9 categories / 18 sub-groups / 70
  subcategories / 10 frameworks). The alignment table and
  narrative don't reflect Agentic AI as a category, the two
  frameworks added in v1.0 (CoE, TC260), or the two added in v1.2
  (CSA Agentic Profile, OWASP Agentic Top 10). Multi-section
  editorial pass; tracked in `meta/STATE.md` of the taxonomy.
- **Calibrate numeric thresholds for MCP-vs-PAT operations**
  (`Eticas-AI/ai-ops` issue #14, opened Fri May 08). Current
  decision rule in `instructions-common.md` is qualitative
  ("under ~500 lines or so as a rough guide"). Plan: 2–3 sessions
  of empirical data collection (file size, operation type,
  observed wall-clock and token cost), then a PR replacing the
  qualitative rule with numeric thresholds.
- **Update local git remote URL for the leaflet repo.** Pablo's
  local clone has `origin` pointing at
  `https://github.com/PabloAccuosto-ETICAS/leaflet.git` — leftover
  from a pre-migration era when the repo lived on the personal
  account. GitHub silently redirects and pushes succeed, but a
  warning surfaces in every push log. One-liner to fix:
  `git remote set-url origin https://github.com/Eticas-AI/leaflet.git`.
- **Optional cleanup: clear Netlify UI Build & Deploy fields for
  the leaflet site.** `netlify.toml` is now the source of truth
  for build command, publish directory, and base directory. The
  UI fields work fine but are misleading because they appear to
  be the configuration when the toml actually overrides them.
  Clearing the UI Base/Command/Publish fields makes the toml
  visibly the only config. Not blocking; do when convenient.
- **Propagate v5 design decisions into the leaflet package**
  (surfaced Sat May 09 with the v5 rationale draft). Three
  concrete pieces: (a) audit-coverage / benchmark-saturation
  field on `AuditReport` + LLM Jinja template + LLM mockup;
  (b) run-repetition policy wired into `metrics_registry.yaml`
  (per-metric-type minimum runs) and the scoring pipeline /
  fixture (record runs alongside severity); (c) review the
  severity-band thresholds in `metrics_registry.yaml` against
  v5's magnitude-of-deviation framing, expose the practical-
  significance margin explicitly per metric. Tracked as priority
  #1 in the leaflet's `meta/STATE.md`.
