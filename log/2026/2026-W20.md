# Week 2026-W20 (May 11 – May 17, 2026)

> Work bootstrapped on Saturday May 09 (W19 day 6), opened in W20
> because the W19 weekly report was already sent. Items dated 09 May
> are entered here ahead of their ISO week.

## Wiselook audit

- **Partner API auth diagnostic + email to Roberto (Sat May 09).**
  Verified keys in `.env` against Roberto's email — match character
  by character. Discriminative test of auth headers via curl: the
  server processes `Authorization: Bearer` and rejects other formats
  with a clear message. Same call via Swagger UI returns HTTP 500
  instead of 401, with the same key. The `-i` flag on curl revealed
  both responses include `x-clerk-auth-*` headers indicating Clerk
  middleware processes the partner API key as a JWT and fails
  (expected — keys are not JWTs); behaviour after Clerk diverges
  by request origin (clean 401 via curl, crash via browser). Email
  sent to Roberto with curl reproducer + full response (CCs include
  Borja Gracia, new contact). Reply pending. Filter probes promoted
  to actionable-now while waiting.
- **Doc-discoverability protocol (Sat May 09).** Mid-session
  realisation: hours of partner-API hypothesising could have been
  short-circuited by reading
  [`docs/context/PARTNER-INTEGRATION-GUIDE.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/docs/context/PARTNER-INTEGRATION-GUIDE.md),
  which has been in the repo since 27 April and indexed in
  `STATE.md`. Three corrections applied to prevent recurrence:
  (1) inline references to relevant docs in each item of `Blocking on
  Wiselook` and `Can move now` of STATE.md, so the pointer sits next
  to the problem; (2) renamed the context-table section to
  `Read before hypothesising` with the rule literally as the heading
  and a short rationale prefacing the table; (3) new
  `Receiving new documentation` protocol in STATE.md (commit + entry
  in `docs/context/README.md` + row in the context table + inline
  reference in active items + INTERNAL_LOG entry) so future docs are
  reachable through three independent paths. Project-instructions
  block in the Claude UI updated with the rule.

For the full chronological detail, see
[`Eticas-AI/wiselook-audit/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/INTERNAL_LOG.md).

## Transversal / Cross-project

- **Contributors directory + revert §3.1 PK cache default to opt-in
  (Sat May 09).** PR
  [Eticas-AI/ai-ops#16](https://github.com/Eticas-AI/ai-ops/pull/16)
  merged. Two changes to the canonical pattern, separated into their
  own commits. (1) Added `contributor-resources.md` at the root of
  `ai-ops`: a thin index mapping a contributor's GitHub handle to
  their personal repo and preferences-file path. The directory does
  not duplicate org membership data (GitHub knows that better) — it
  only holds the Eticas-specific handle → personal-repo +
  preferences-path mapping. Added an explicit rule in
  `instructions-common.md` and the §5 template forbidding the
  construction of handles from a contributor's name. Trigger: a
  Wiselook session start in which Claude proposed a plausible-looking
  but invented handle (`pgsaez-ETICAS`); the multi-shape reality on
  the org (`PabloAccuosto-ETICAS`, `UsmanGohar-Eticas`,
  `gemma-eticas`, `noeamoedo`) makes guessing unsafe. (2) §3.1 PK
  cache default reverted from default-on (PR #6 of 2026-05-06) to
  opt-in. Two days of operational evidence showed 5 of 6 active
  projects had either explicitly opted out or implicitly ignored the
  default (Wiselook bootstrap never completed). Lesson recorded:
  defaults requiring ongoing active coordination need evidence the
  coordination actually happens before being declared default.
- **Doc-discoverability observation logged in `ai-ops` (Sat May 09).**
  Commit `be553c0`. The Wiselook session's failure to consult an
  in-repo doc indexed in STATE.md was logged as an observation worth
  monitoring across projects. No canonical-pattern change proposed
  yet — one observation isn't enough evidence. If the same failure
  mode surfaces in another project, promote the inline-references
  and ingest-protocol patterns from Wiselook to
  `claude-project-pattern.md` §2 as guidance for STATE.md structure.
  Tracked in `inventory.md` under Pending operational TODOs.
- **Observation logged on `INTERNAL_LOG.md` growth (Sat May 09).**
  Commit `00bc5ab`. Append-only is the right invariant for historical
  record but produces fetch costs that grow indefinitely; PR #16
  ruled out the PK cache as a default response. Three possible
  directions noted (periodic archival, promotion-as-deletion,
  splitting buffer-style sections into their own file) but not
  decided. Tracked as a pending TODO in `inventory.md`.

- **Inventory updates and platform priority TODO (Mon May 11).**
  Three small commits to [`Eticas-AI/ai-ops/inventory.md`](https://github.com/Eticas-AI/ai-ops/blob/main/inventory.md)
  after today's project work landed. (1) New "Strategy" row in
  Active projects registering `Eticas-AI/strategy` (Career Scoops
  shape, scope explicit). (2) Methodology row version bumped
  `v0.3.3 → v0.3.5` after the two methodology PRs merged. (3) New
  first item in "Pending operational TODOs": **Bootstrap the
  platform project — priority**, with notes on the open decision
  about whether the HR/credit dataset guides currently in
  `ai-audit-methodology/testing/` stay there, move to the platform
  repo, or are referenced from both. The platform workstream had
  been listed generically under "Other workstreams — separate
  future migrations" in the methodology bootstrap entry; this
  flagging surfaces it as the next priority migration after today's
  strategy bootstrap.

For the full chronological detail, see
[`Eticas-AI/ai-ops/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-ops/blob/main/meta/INTERNAL_LOG.md).

## Audit methodology

- **Migration of the methodology workstream to a dedicated Claude project
  (Mon May 11).** PR
  [Eticas-AI/ai-audit-methodology#1](https://github.com/Eticas-AI/ai-audit-methodology/pull/1)
  merged (commit `f5a0214` on `main`). Until now the methodology repo
  had been bootstrapped to v0.3.3 (README, TRACKER, CHANGELOG, four-layer
  `layers/` tree partially populated) but lacked a `meta/` layer and a
  dedicated Claude project — methodology work had been happening inside
  a broader project mixing several distinct workstreams (methodology,
  leaflet, taxonomy, Career Scoops audit, datasets research, business
  strategy, R&D proposals, HRA, data space, website copy, Wiselook
  context, etc.). The bootstrap added `meta/` with STATE, CONTEXT,
  INTERNAL_LOG and README following the canonical pattern, and surfaced
  `meta/STATE.md` plus the parent SharePoint folder in the repo's main
  README. Shape chosen: Career Scoops (PK empty — repo holds structured
  content; SharePoint canonical for source DOCX via M365 MCP). Inventory
  in `ai-ops` updated to reflect the new shape (commit `5624f74`).
  Migration ran against the canonical pattern and runbook with no new
  lessons surfaced — the runbook's third-case scenario (repo exists, no
  dedicated Claude project) covered the situation. Next migration in
  this series: the Leaflet project (`Eticas-AI/leaflet`).

- **Methodology refinements — audit-report philosophy and external
  references (Mon May 11).** Two PRs merged in the afternoon.
  [#2](https://github.com/Eticas-AI/ai-audit-methodology/pull/2)
  (v0.3.4) folded an "Audit report philosophy" document into Layer 1
  as a new component `layers/layer-1-foundations/audit-report/`,
  capturing Eticas' transversal principles for how audit reports are
  written (structure I–V, methods/findings separation, recommendations
  as a verifiable checklist, limitations as findings). Status: draft
  for validation with Gemma — marked explicitly in the doc and in the
  subdirectory README. The Layer 1 README "Report templates"
  subsection became "Audit report — philosophy and templates",
  pointing at the new subdirectory.
  [#3](https://github.com/Eticas-AI/ai-audit-methodology/pull/3)
  (v0.3.5) added three external-reference consolidations: the
  BiasAsker-RAG extension proposal as a versioned R&D reference in
  `docs/`; a new "Related references" section in the README pointing
  at the RAIA Guide (Eticas' methodology for GIZ's FAIR Forward
  programme, source canonical in SharePoint); and the
  `Eticas-AI/leaflet` repo added to "Related repositories" alongside
  `ai-risk-taxonomy`. Methodology Claude project memory update
  drafted (manual paste pending) to reflect the post-bootstrap scope.

For the full chronological detail, see
[`Eticas-AI/ai-audit-methodology/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-audit-methodology/blob/main/meta/INTERNAL_LOG.md).

## Strategy

- **Strategy workstream bootstrapped as a dedicated project
  (Mon May 11).** Initial commit on
  [`Eticas-AI/strategy`](https://github.com/Eticas-AI/strategy)
  (commit `e164dd3` on `main`, repo created today). Following the
  methodology bootstrap earlier in the morning, the strategy
  workstream was the next in line per the methodology
  `INTERNAL_LOG` migration roadmap. Repo initialised with the full
  layered meta-state (README, `meta/` with STATE/CONTEXT/
  INTERNAL_LOG/README, root TRACKER); three working artefacts
  migrated from the broader source project's PK to `docs/`:
  `business-strategy.md` (V1.0 Proposal by Noelia, April 2026),
  `competitive-landscape.md` (AI governance market analysis of 13
  competitors), `competitors.xlsx` (working competitor database
  with ecosystem-level defensibility analysis). Shape chosen:
  Career Scoops — PK empty, SharePoint Growth folder
  ([`eticastech.sharepoint.com/sites/Growth/`](https://eticastech.sharepoint.com/sites/Growth/))
  canonical for the broader, team-owned strategy assets
  (Positioning Document approved, Communication Strategy WIP).
  Scope made explicit in `meta/CONTEXT.md` and `TRACKER.md`:
  Eticas.ai commercial strategy (positioning, GTM, competitive
  intelligence, pricing, partnerships); research funding
  workstreams (NEOTEC, Eurostars, data space) remain out of scope
  and deferred to future bootstraps. Additional strategy-relevant
  material identified in a parallel Claude account
  (`Validation_of_Value_Proposition`, `eticas_competitive_analysis`,
  possibly `Eticas_Website_Copy_v3`) — migration deferred to a
  follow-up session in the new strategy project once uploaded.
  Gates Foundation files initially considered for strategy are
  actually Career Scoops deliverables and belong to
  `Eticas-AI/career-scoops-audit`.

- **First substantive session: scope narrowed, open commercial-model
  question recorded (Mon May 11).** Three commits to
  [`Eticas-AI/strategy`](https://github.com/Eticas-AI/strategy)
  (`ab40c99`, `beddf21`, `46199c2` on `main`). First contact with
  reality after the morning bootstrap revised the repo's role: rather
  than hold version-controlled copies of Noelia's strategy documents
  (which iterate in SharePoint at a cadence that makes repo copies
  stale within days), `docs/business-strategy.md` was reduced to an
  annotated stub mapping the live SharePoint material (Business
  Strategy 2026, Validation of Value Proposition, HR Messaging,
  Literature support, Competitor list), `docs/competitors.xlsx`
  removed, and the repo's role re-framed as a team thinking space
  for decisions, repo-side analyses, drafts and exploratory work —
  not a mirror of Noelia's workspace. The `[TBC]` placeholder for
  `Current focus` in STATE.md was resolved to two threads: Noelia's
  Validation of Value Proposition cycle, and a newly-opened question
  on commercial-model direction (potential shift from audit-led
  delivery toward continuous-monitoring delivery, most realistically
  in partnership with observability providers rather than by building
  infrastructure). The latter was prompted by Gemma's 8 May
  "Learning from AI observability" thread and the prior 29 April
  "Product roadmap" thread, with the operational ceiling on blind
  external LLM audits (Wiselook is the live example) as background.
  Registered in TRACKER as an explicit open question, not yet a
  decision — to be discussed in the team meeting later today.
  Reply drafted to Gemma and Noelia acknowledging the diagnosis,
  endorsing Noelia's "complement observability providers" framing
  as the realistic step given current build capacity, and deferring
  detailed architectural decisions.

For the full chronological detail, see
[`Eticas-AI/strategy/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/strategy/blob/main/meta/INTERNAL_LOG.md).
