# Week 2026-W20 (May 11 – May 17, 2026)

> Work bootstrapped on Saturday May 09 (W19 day 6), opened in W20
> because the W19 weekly report was already sent. Items dated 09 May
> are entered here ahead of their ISO week.

## Wiselook audit

- **Partner API auth diagnostic + email to Roberto (Sat May 09).**
  Verified keys in `.env` against Roberto's email — match character
  by character. Discriminative test of auth headers via curl: the
  server processes `Authorization: Bearer` and rejects other formats
  with a clear message. Same call via Swagger UI returns HTTP 500
  instead of 401, with the same key. The `-i` flag on curl revealed
  both responses include `x-clerk-auth-*` headers indicating Clerk
  middleware processes the partner API key as a JWT and fails
  (expected — keys are not JWTs); behaviour after Clerk diverges
  by request origin (clean 401 via curl, crash via browser). Email
  sent to Roberto with curl reproducer + full response (CCs include
  Borja Gracia, new contact). Reply pending. Filter probes promoted
  to actionable-now while waiting.
- **Doc-discoverability protocol (Sat May 09).** Mid-session
  realisation: hours of partner-API hypothesising could have been
  short-circuited by reading
  [`docs/context/PARTNER-INTEGRATION-GUIDE.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/docs/context/PARTNER-INTEGRATION-GUIDE.md),
  which has been in the repo since 27 April and indexed in
  `STATE.md`. Three corrections applied to prevent recurrence:
  (1) inline references to relevant docs in each item of `Blocking on
  Wiselook` and `Can move now` of STATE.md, so the pointer sits next
  to the problem; (2) renamed the context-table section to
  `Read before hypothesising` with the rule literally as the heading
  and a short rationale prefacing the table; (3) new
  `Receiving new documentation` protocol in STATE.md (commit + entry
  in `docs/context/README.md` + row in the context table + inline
  reference in active items + INTERNAL_LOG entry) so future docs are
  reachable through three independent paths. Project-instructions
  block in the Claude UI updated with the rule.

- **Partner API unblocked, LangGraph STG identified as new blocker
  (Mon May 11).** Roberto rotated the partner API keys with a new
  format (`wskp-eti01-` PROD, `wsks-eti01-` STG); both authenticate
  and the STG smoke test passes end-to-end — `POST /partner/start`
  materialises a `userEvaluationId` without anyone opening the magic
  link. The 9 May Clerk-fallback vs key-value rejection puzzle is
  resolved retrospectively: the old keys were genuinely invalid, the
  format change explains why character-by-character verification of
  the original delivery didn't help. A subsequent empirical experiment
  (designed before asking Roberto: cross-test a partner-STG
  `userEvaluationId` against the only LangGraph URL we have) returned
  `UserEvaluation <id> not found` from the Claire graph — confirming
  there are two separate LangGraph Cloud deployments, one per
  environment, and the URL we have atiende only PROD. Email to Roberto
  sent asking for (1) the STG deployment URL and (2) whether the
  existing `WISELOOK_LANGGRAPH_API_KEY` works for STG or a separate
  key is needed. Reply pending; orchestrator-parameterisable skeleton
  is the work that can proceed in parallel.

For the full chronological detail, see
[`Eticas-AI/wiselook-audit/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/INTERNAL_LOG.md).

## Transversal / Cross-project

- **Contributors directory + revert §3.1 PK cache default to opt-in
  (Sat May 09).** PR
  [Eticas-AI/ai-ops#16](https://github.com/Eticas-AI/ai-ops/pull/16)
  merged. Two changes to the canonical pattern, separated into their
  own commits. (1) Added `contributor-resources.md` at the root of
  `ai-ops`: a thin index mapping a contributor's GitHub handle to
  their personal repo and preferences-file path. The directory does
  not duplicate org membership data (GitHub knows that better) — it
  only holds the Eticas-specific handle → personal-repo +
  preferences-path mapping. Added an explicit rule in
  `instructions-common.md` and the §5 template forbidding the
  construction of handles from a contributor's name. Trigger: a
  Wiselook session start in which Claude proposed a plausible-looking
  but invented handle (`pgsaez-ETICAS`); the multi-shape reality on
  the org (`PabloAccuosto-ETICAS`, `UsmanGohar-Eticas`,
  `gemma-eticas`, `noeamoedo`) makes guessing unsafe. (2) §3.1 PK
  cache default reverted from default-on (PR #6 of 2026-05-06) to
  opt-in. Two days of operational evidence showed 5 of 6 active
  projects had either explicitly opted out or implicitly ignored the
  default (Wiselook bootstrap never completed). Lesson recorded:
  defaults requiring ongoing active coordination need evidence the
  coordination actually happens before being declared default.
- **Doc-discoverability observation logged in `ai-ops` (Sat May 09).**
  Commit `be553c0`. The Wiselook session's failure to consult an
  in-repo doc indexed in STATE.md was logged as an observation worth
  monitoring across projects. No canonical-pattern change proposed
  yet — one observation isn't enough evidence. If the same failure
  mode surfaces in another project, promote the inline-references
  and ingest-protocol patterns from Wiselook to
  `claude-project-pattern.md` §2 as guidance for STATE.md structure.
  Tracked in `inventory.md` under Pending operational TODOs.
- **Observation logged on `INTERNAL_LOG.md` growth (Sat May 09).**
  Commit `00bc5ab`. Append-only is the right invariant for historical
  record but produces fetch costs that grow indefinitely; PR #16
  ruled out the PK cache as a default response. Three possible
  directions noted (periodic archival, promotion-as-deletion,
  splitting buffer-style sections into their own file) but not
  decided. Tracked as a pending TODO in `inventory.md`.

- **Inventory updates and platform priority TODO (Mon May 11).**
  Three small commits to [`Eticas-AI/ai-ops/inventory.md`](https://github.com/Eticas-AI/ai-ops/blob/main/inventory.md)
  after today's project work landed. (1) New "Strategy" row in
  Active projects registering `Eticas-AI/strategy` (Career Scoops
  shape, scope explicit). (2) Methodology row version bumped
  `v0.3.3 → v0.3.5` after the two methodology PRs merged. (3) New
  first item in "Pending operational TODOs": **Bootstrap the
  platform project — priority**, with notes on the open decision
  about whether the HR/credit dataset guides currently in
  `ai-audit-methodology/testing/` stay there, move to the platform
  repo, or are referenced from both. The platform workstream had
  been listed generically under "Other workstreams — separate
  future migrations" in the methodology bootstrap entry; this
  flagging surfaces it as the next priority migration after today's
  strategy bootstrap.

- **Contributor onboarding docs added to `ai-ops` (Wed May 13).** PR
  [Eticas-AI/ai-ops#17](https://github.com/Eticas-AI/ai-ops/pull/17)
  opened. Two new files at the root of `ai-ops`:
  `contributor-guide.md`, a one-page operational onboarding covering
  quick start, session shape, the personal preferences file, the
  Claude project's instructions block, GitHub access (connector vs
  PAT), and how to start a project of your own; and
  `claude-personal-preferences.example.md`, an example of the
  personal preferences file each contributor maintains in their own
  Eticas personal repo, with explanatory framing of what these
  files are for, what goes in them, and what doesn't, followed by a
  copyable template body. README updated to list the two new files
  in "What lives in this repo"; the two direct "Talk to Pablo"
  references in "How to get started" step 1 and the Maintenance
  closing line removed for consistency with the new guide's
  individual-agnostic framing. Trigger: drafting an on-ramp for a
  new contributor surfaced that the README is conceptual,
  `instructions-common.md` is written for Claude, and the migration
  runbook is for project setup — none was a single-page operational
  onboarding. Both files written for any future contributor.

For the full chronological detail, see
[`Eticas-AI/ai-ops/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-ops/blob/main/meta/INTERNAL_LOG.md).

## Audit methodology

- **Migration of the methodology workstream to a dedicated Claude project
  (Mon May 11).** PR
  [Eticas-AI/ai-audit-methodology#1](https://github.com/Eticas-AI/ai-audit-methodology/pull/1)
  merged (commit `f5a0214` on `main`). Until now the methodology repo
  had been bootstrapped to v0.3.3 (README, TRACKER, CHANGELOG, four-layer
  `layers/` tree partially populated) but lacked a `meta/` layer and a
  dedicated Claude project — methodology work had been happening inside
  a broader project mixing several distinct workstreams (methodology,
  leaflet, taxonomy, Career Scoops audit, datasets research, business
  strategy, R&D proposals, HRA, data space, website copy, Wiselook
  context, etc.). The bootstrap added `meta/` with STATE, CONTEXT,
  INTERNAL_LOG and README following the canonical pattern, and surfaced
  `meta/STATE.md` plus the parent SharePoint folder in the repo's main
  README. Shape chosen: Career Scoops (PK empty — repo holds structured
  content; SharePoint canonical for source DOCX via M365 MCP). Inventory
  in `ai-ops` updated to reflect the new shape (commit `5624f74`).
  Migration ran against the canonical pattern and runbook with no new
  lessons surfaced — the runbook's third-case scenario (repo exists, no
  dedicated Claude project) covered the situation. Next migration in
  this series: the Leaflet project (`Eticas-AI/leaflet`).

- **Methodology refinements — audit-report philosophy and external
  references (Mon May 11).** Two PRs merged in the afternoon.
  [#2](https://github.com/Eticas-AI/ai-audit-methodology/pull/2)
  (v0.3.4) folded an "Audit report philosophy" document into Layer 1
  as a new component `layers/layer-1-foundations/audit-report/`,
  capturing Eticas' transversal principles for how audit reports are
  written (structure I–V, methods/findings separation, recommendations
  as a verifiable checklist, limitations as findings). Status: draft
  for validation with Gemma — marked explicitly in the doc and in the
  subdirectory README. The Layer 1 README "Report templates"
  subsection became "Audit report — philosophy and templates",
  pointing at the new subdirectory.
  [#3](https://github.com/Eticas-AI/ai-audit-methodology/pull/3)
  (v0.3.5) added three external-reference consolidations: the
  BiasAsker-RAG extension proposal as a versioned R&D reference in
  `docs/`; a new "Related references" section in the README pointing
  at the RAIA Guide (Eticas' methodology for GIZ's FAIR Forward
  programme, source canonical in SharePoint); and the
  `Eticas-AI/leaflet` repo added to "Related repositories" alongside
  `ai-risk-taxonomy`. Methodology Claude project memory update
  drafted (manual paste pending) to reflect the post-bootstrap scope.

- **Methodology refinement session — glossary, hierarchical
  aggregation model, DecodingTrust as validation benchmark
  (Wed May 13).** Working session originally aimed at producing
  concrete YAML for a DecodingTrust example pivoted mid-stream to
  using DecodingTrust as a vehicle for surfacing methodological
  gaps. Seven commits to `Eticas-AI/ai-audit-methodology` (main)
  across Wed–Thu landing v0.3.7. Three substantive methodology
  pieces: a controlled-vocabulary glossary at
  [`layers/layer-1-foundations/glossary.md`](https://github.com/Eticas-AI/ai-audit-methodology/blob/main/layers/layer-1-foundations/glossary.md)
  (27 entries fixing the meaning of terms like experiment, run,
  replication, protocol, test case, metric across methodology,
  taxonomy, and the leaflet package); the hierarchical grade
  aggregation model spec at
  [`layers/layer-1-foundations/leaflet/aggregation-model.md`](https://github.com/Eticas-AI/ai-audit-methodology/blob/main/layers/layer-1-foundations/leaflet/aggregation-model.md)
  (Dimension → Subcategory → Check; subcategory grade = peak
  severity of its checks; subcategory pattern flag
  ISOLATED/FOCAL/SYSTEMIC/null; dimension grade = peak + breadth
  over subcategory grades; coverage indicator — target spec the
  leaflet package should converge to from its current flat-model
  implementation); and a new top-level `examples/` directory with
  `examples/decodingtrust/` bootstrapped (top-level README mapping
  six DecodingTrust perspectives to six canonical subcategories
  across four dimensions; per-risk subdirectories TBD, recommended
  start `pii-leakage/`). Two supporting changes:
  `testing/datasets-benchmarks.md` updated to invert the
  primary/complement benchmark choice (DecodingTrust primary,
  AILuminate complement specifically for Security & Misuse depth
  — AILuminate's hazard distribution is too skewed to security and
  its pre-derived A–F grades would short-circuit the
  operationalisation step the methodology needs to demonstrate);
  and Gemma's 2026-05-09 operationalisation template draft
  preserved verbatim at
  `docs/decisions/0001-attachments/operationalisation-template-draft.md`
  as ADR-0001 attachment with a framing header (the file had been
  in this project's PK at session start and was about to be removed
  based on STATE.md; durable preservation is the fix — lesson:
  when STATE flags a PK item for removal, verify content
  preservation in the repo before trusting the flag).
  Coordination with the `leaflet` repo deferred:
  aggregation-model.md implies substantive package changes (new
  SubcategoryFindings entity, SubcategoryGrade type, Pattern enum,
  split aggregation functions, coverage indicator entity,
  backward-compat for Wiselook synthetic fixture which uses
  non-canonical URIs); no PR or issue opened. Handover document
  prepared for project knowledge. Next concrete output: first
  per-risk subdirectory of `examples/decodingtrust/` end-to-end.

- **Audit findings architecture decided + inventory pass
  (Thu May 14).** Session opened writing the first DecodingTrust
  subdirectory end-to-end (`examples/decodingtrust/pii-leakage/`)
  as the pattern reference, then pivoted to resolving a deeper
  architectural ambiguity surfaced by the work: where do grades
  live? where does the aggregation engine live? what's the
  relationship between the structured audit data, the leaflet, the
  client-facing narrative report, and anticipated additional
  consumers (platform, regulatory submission)? Three commits to
  `Eticas-AI/ai-audit-methodology` (main): `01771a2` (pii-leakage
  subdirectory end-to-end — README + audit-snippet.yaml,
  hierarchical-model shape, severity bands draft pending Gemma);
  `3327224` (ADR-0002 + the canonical audit-findings architecture
  document at `docs/architecture/audit-findings-architecture.md`);
  `fc855bd` (per-file inventory at
  `meta/audit-findings-architecture-inventory.md` classifying 43
  methodology files and 15 leaflet repo `.md` files against
  ADR-0002, with a five-batch reconciliation plan). Seven
  architectural principles ratified: audit findings (renamed from
  "audit report") is the SSOT; consumers project not generate;
  aggregation rules belong to the methodology; text comes at three
  predefined densities (`headline`/`summary`/`narrative`); consumer
  manifests govern projection; audit findings carries no
  consumer-targeting flags; schema + aggregation engine ship with
  the methodology. Storage moved from single YAML to a directory of
  YAMLs (refines on first real engagement migration, likely
  Wiselook). The architecture supersedes the linear v4 diagram at
  `eticas-leaflet.netlify.app/architecture/leaflet-architecture-diagram-v4`
  (working draft, 12 March 2026) and the implicit naming overload
  of "audit report" (now: audit findings = structured YAML;
  client report = narrative document delivered to the client, one
  consumer of N). Schema migration from `Eticas-AI/leaflet/src/` to
  this repo is Phase 4 (separate workstream after the `.md`
  reconciliation pass). End-of-session deltas pushed in two further
  commits (`3ad0158` STATE.md, `413eb34` INTERNAL_LOG.md entry).
  Handover document produced for upload to PK to make next-session
  pickup low-overhead.

- **Phase 3 reconciliation (ADR-0002) executed end-to-end across both
  repos (Thu May 15).** Working session executing the five-batch
  Phase 3 plan recorded the previous day in
  `meta/audit-findings-architecture-inventory.md`. All five batches
  landed: 4 PRs (3 in methodology, 1 in leaflet), 22 commits across
  both repos, 38 files touched. Batch A
  ([#4](https://github.com/Eticas-AI/ai-audit-methodology/pull/4)) —
  directory renames (`audit-report/` → `client-report/`,
  `leaflet/aggregation-model.md` →
  `aggregation/aggregation-model.md`), top-level README multi-consumer
  star Mermaid, glossary extended with architecture vocabulary
  (audit findings / client report / consumer / consumer manifest /
  multi-density text and the three density entries; "audit report"
  deprecated as entry 17b), `leaflet/rationale.md` (V4) banners
  marking §2 as superseded. Batch B
  ([#5](https://github.com/Eticas-AI/ai-audit-methodology/pull/5)) —
  example fixture renamed `audit-snippet.yaml` →
  `audit-findings-snippet.yaml` with `leaflet_export` flags dropped
  as the in-repo demonstration of ADR-0002 Principle 6;
  `pii-leakage/` and `examples/decodingtrust/` READMEs reframed;
  `meta/CONTEXT.md` Production flow rewritten. Batch C
  ([#6](https://github.com/Eticas-AI/ai-audit-methodology/pull/6)) —
  `docs/glossary.md` reduced to redirect stub;
  `docs/layered-architecture.md` Layer 1 components and Production
  flow rewritten; ADR-0001 + attachment small terminology updates;
  TRACKER and CHANGELOG new v0.3.8 entries plus surgical annotations
  on the v0.3.4 audit-report-philosophy and v0.3.7
  hierarchical-aggregation-model entries (preserved as historical
  record with `— superseded by X` suffixes and path-reference
  updates so links resolve). `testing/hr-datasets.md` and
  `testing/credit-datasets.md` verified-and-left-untouched (all
  matches are external industry references — NYC LL144, HireVue,
  Aequitas, ProPublica — using "audit report" in its proper
  external sense). Batch D
  ([leaflet #11](https://github.com/Eticas-AI/leaflet/pull/11)) —
  the leaflet repo's twelve `.md` files reconciled: `README.md` and
  `docs/package-architecture.md` substantial reframes with
  multi-consumer star Mermaid and Phase 4 banner; smaller terminology
  + Phase-4-framing edits across walkthrough, context-summary,
  metrics, src/examples READMEs; new top entry in leaflet
  `TRACKER.md` anchoring this repo's role under the new architecture;
  meta touchpoints written additively only (`meta/STATE.md` banner,
  `meta/CONTEXT.md` Position-in-the-audit-pipeline rewritten,
  `meta/INTERNAL_LOG.md` new appended entry — with the explicit
  framing that the leaflet's own Claude project sessions retain
  ownership of operational state from here, this methodology session
  signs off on the leaflet repo's meta from its side and the
  leaflet's project takes it from there). Batch E
  ([#7](https://github.com/Eticas-AI/ai-audit-methodology/pull/7)) —
  cleanup catches Batch C should have done but didn't (sweep caught
  `layers/layer-2-core/llm/methodology-review.md` ACC-3 row +
  header banner with ADR-0002 cross-link; `testing/datasets-benchmarks.md`
  §"leaflet pipeline" renamed to §"audit findings pipeline" with
  Phase 4 callouts in the input/output bullets) plus inventory
  closure: aggregate-counts recount post-execution showing 39 of 43
  methodology entries Aligned and 12 of 15 leaflet entries Aligned;
  Phase 3 completion summary section with per-batch landing record,
  cumulative outputs, and "What landed and what didn't" detailing
  Phase 4 deferrals (code under `src/leaflet/`, Wiselook fixture
  migration to multi-file `audit-findings/` directory, schema +
  aggregation engine migration) and downstream-of-Phase-4 deferrals
  (v5 architecture diagram, v6 rationale); status banner updated
  to "Phase 3 complete (2026-05-15)". One operational detail worth
  remembering for future multi-batch rollouts: Batch E was branched
  off Batch C's feature branch (rather than `main`) so the cleanup
  pass + inventory recount could see post-C state; when PR #6 (C)
  merged and its branch was auto-deleted, PR #7's base validation
  initially failed against the now-missing branch, then retargeting
  to `main` (now containing C) produced a clean 2-commit diff.
  Methodology repo at v0.3.8 with TRACKER + CHANGELOG entries
  reflecting the architectural anchor; cumulative state post-Phase-3
  has the vocabulary settled, directory layout finalised,
  `examples/decodingtrust/pii-leakage/` as the canonical pattern
  reference, and architecture spec + ADR-0002 cross-linked from
  every primary entry point. End-of-session deltas pushed: methodology
  repo `meta/STATE.md` substantially rewritten and
  `meta/INTERNAL_LOG.md` new entry appended; W20 entry (this one);
  handover documents produced for upload to both the methodology
  and leaflet PKs. Unblocked from here: DecodingTrust continuation
  (`bias-fairness/` is the natural next subdirectory) and the lift
  of per-subcategory invariants from `pii-leakage/` to Layer 2
  operationalisation (parallel-able with the DecodingTrust work,
  no inventory dependency). Phase 4 (schema and aggregation engine
  migration from `Eticas-AI/leaflet/src/` to the methodology repo)
  is the next major workstream — warrants its own ADR-0003 +
  inventory before execution.

- **Architecture day on top of Phase 3 — ADR-0003 + ADR-0004 +
  Phase 4 inventory (Fri May 15, evening).** Five Tandas executed
  sequentially in `Eticas-AI/ai-audit-methodology`, all commits
  to `main`, all documentation/decisions (no code).
  [`05d2c48`](https://github.com/Eticas-AI/ai-audit-methodology/commit/05d2c48)
  moved Claude-assisted datasets/benchmarks research from PK to
  `meta/datasets-benchmarks-buffer.md`, parallel pattern to
  `tools-catalog-buffer.md`.
  [`b61f435`](https://github.com/Eticas-AI/ai-audit-methodology/commit/b61f435)
  closed a status ambiguity across five places in the repo
  reframing `layers/layer-2-core/llm/operationalization/{fairness,reliability}.md`
  as exploratory first attempts, not decided architecture, with
  an annotation block appended to ADR-0001 qualifying its
  "canonical home" wording.
  [`a4887f3`](https://github.com/Eticas-AI/ai-audit-methodology/commit/a4887f3)
  adopted
  [**ADR-0003**](https://github.com/Eticas-AI/ai-audit-methodology/blob/main/docs/decisions/0003-layered-operationalisation.md)
  *Layered operationalisation* with six concrete decisions: defaults
  at Layer 2, slot-level overrides at Layer 3, instance application
  + optional instance override at Layer 4 with mandatory rationale,
  provenance recorded on every check (layer of origin, override
  status, rationale), severity bands as Layer 2 invariants (current
  `pii-leakage/` pattern declaring bands per-instance is incorrect),
  provenance explicit even when no override. Renumbering: what was
  planned as ADR-0003 for Phase 4 schema migration is now ADR-0004.
  [`15bedfc`](https://github.com/Eticas-AI/ai-audit-methodology/commit/15bedfc)
  adopted
  [**ADR-0004**](https://github.com/Eticas-AI/ai-audit-methodology/blob/main/docs/decisions/0004-phase-4-schema-migration.md)
  *Phase 4 schema migration* with seven sub-decisions fixing the
  concrete shapes: L2 entry = one YAML file per subcategory at
  `layers/layer-2-core/<technology>/operationalization/<subcategory-id>.yaml`;
  L3 override entry = per (sector, technology) with operation enum
  (`replace`/`add`/`remove`) + slot + value + rationale; L4 Pydantic
  schema with new types (`SubcategoryFindings`, `Pattern`, `Provenance`,
  `SubcategoryGrade`), multi-density text at audit/dimension/
  subcategory levels, removal of `leaflet_export` flag; L4 directory
  layout confirmed; aggregation engine split into intra-subcategory
  and intra-dimension functions; cross-repo migration of models +
  scoring from `Eticas-AI/leaflet/src/leaflet/` to this repo's
  `src/methodology/`; Wiselook fixture migration out of scope of
  Phase 4 per session decision. Five-batch sequencing summarised
  in the ADR.
  [`f8e2c35`](https://github.com/Eticas-AI/ai-audit-methodology/commit/f8e2c35)
  wrote the per-batch per-file execution plan at
  [`meta/phase-4-schema-migration-inventory.md`](https://github.com/Eticas-AI/ai-audit-methodology/blob/main/meta/phase-4-schema-migration-inventory.md):
  ~19 file creates, ~15 modifies, 3 deletes across both repos;
  five open decisions captured with recommendations (D1 Python
  package name → D5 pyproject scaffolding cadence); pre-execution
  checklist. Glossary grew 33 → 42 entries in three architectural
  blocks (28–33 architecture, 34–39 layered operationalisation,
  40–42 file formats). Repo at v0.3.8 still (version bump deferred
  to Phase 4 Batch 4 cross-repo cut-over).

  Two side artefacts produced for delivery, not committed: a PK
  note for the risk-taxonomy Claude project documenting the
  pending rename of the taxonomy's `operationalisation` field to
  `mechanisms` (lexical collision with the methodology-side term);
  and a figure for Gemma's risk-taxonomy paper illustrating the
  open-core hookup between public taxonomy and private methodology
  using `disparate-impact-protected-groups` as worked example
  (snippet A real taxonomy entry, snippet B illustrative methodology
  L2 entry with concept descriptors named and specific values
  redacted). Next session expected to start Phase 4 Batch 1 (L2
  entry for `pii-leakage` cosmetic lift + L3 stub directory) once
  the inventory's pre-execution checklist clears.

For the full chronological detail, see
[`Eticas-AI/ai-audit-methodology/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-audit-methodology/blob/main/meta/INTERNAL_LOG.md).

## Leaflet

- **Operationalisation home decided across the three repos (Mon May 11,
  ADR-0001 in methodology).** Gemma had shared a Claude-drafted
  operationalisation template that packed definition + framework
  mappings + test approach + metrics + thresholds + tools + benchmarks
  + sector notes + leaflet rendering into a single per-risk YAML
  block; the methodology repo's own `STATE.md` was already pointing
  the same direction (migrate Layer 2 LLM operationalisation drafts
  into the taxonomy). This session's decision reverses that: ontological
  fields stay in `ai-risk-taxonomy`, process fields live in
  `ai-audit-methodology`, output-rendering fields live in the leaflet
  repo. Captured as
  [`ai-audit-methodology` ADR-0001](https://github.com/Eticas-AI/ai-audit-methodology/blob/main/docs/decisions/0001-operationalisation-fields-split-across-repos.md)
  (commit `608f1f0`), with methodology's STATE/TRACKER/CHANGELOG
  updated; the leaflet repo carries a cross-reference entry in
  `TRACKER.md` (commit `4587b19`) so future leaflet sessions don't
  re-litigate.

- **v5 design propagated into the package
  ([PR #8](https://github.com/Eticas-AI/leaflet/pull/8), merge `f3313b9`).**
  Six commits implementing the three v5 design changes that previously
  lived only in the rationale prose: audit-coverage /
  benchmark-saturation field on `MetricCheck` (with `BenchmarkInfo`
  sub-model), run-repetition policy (`runs` on `MetricCheck` +
  `minimum_runs` per stochastic metric in
  `metrics_registry.yaml`), and practical-significance margin declared
  explicitly per metric in the registry. Wiselook-0.1 synthetic
  fixture updated with `n_test_cases` and `runs` on five MetricChecks;
  committed `examples/wiselook-0.1-synthetic/leaflet.html` regenerated
  with the new panel; 14 new tests (42 → 56 passing). Session
  defaults for non-fairness margins approved and marked `[TBC]` in
  YAML: rate 3pp, ratio 0.05, variance 5% of range. Threshold-band
  recalibration against the now-explicit margin and runtime
  validation of `runs >= minimum_runs` explicitly out of scope of
  this pass.

- **Self-contained examples convention
  ([PR #9](https://github.com/Eticas-AI/leaflet/pull/9), merged).**
  Pablo's observation: the TRACKER 2026-05-08 entry declaring
  `examples/<name>/` cases as "self-contained directories" was
  broken by the fact that the input audit-report YAML lived
  separately under `data/synthetic/`. `git mv` of
  `data/synthetic/wiselook-0.1-audit.yaml` to
  `examples/wiselook-0.1-synthetic/audit-report.yaml`; `data/`
  removed entirely. Filename by-role (consistent with
  `leaflet.html`, `meta.yaml`, `README.md`); case identity carried
  by the directory name. Side-effect on the CLI default output path
  (`output/<input-stem>.html` → `output/audit-report.html`) noted
  but accepted. References updated across 4 READMEs + tests + STATE.

- **Package documentation landed
  ([PR #10](https://github.com/Eticas-AI/leaflet/pull/10), merge `92a140a`).**
  Two new docs under `docs/`: `package-architecture.md` (module map
  + 4 Mermaid diagrams: pipeline, AuditReport composition, Leaflet
  downstream shape, ecosystem position; with an explicit "What is
  NOT in the package" section ataja-ing common assumptions),
  `walkthrough.md` (end-to-end trace through the Wiselook-0.1
  fixture, Bias & Fairness as the illustrative dimension with the
  firing rule explained). Both READMEs reframed from "implements v4,
  v5 pending" to "implements v5"; stale link to
  `docs/html/templates/` (renamed to `mockups/` on 2026-05-08)
  fixed.

- **Operational asides, not closed today.** Netlify deploy previews
  on PRs blocked throughout the session by the free-plan
  organization-owned-private-repo gate; production deploys also
  blocked via the integration. Pablo's CLI deploy attempt produced
  an error he didn't pursue further; the live site at
  `eticas-leaflet.netlify.app` is desynced from `main` until a
  deploy can run. Three forward paths surfaced (Netlify Pro,
  transition repo to public in sync with Gemma's visibility
  evolution plan, switch to CLI-only deploys) — decision deferred,
  to be handled deliberately rather than as a side-effect.
  Separately: Gemma had GitHub MCP access issues against multiple
  Eticas-AI private repos in Claude Desktop. Diagnosed as
  configuration of the MCP server / Connector rather than per-repo
  permissions (she's org admin, has admin permission on the repos
  by inheritance); the Anthropic-managed GitHub Connector via
  `Settings → Connectors` is the simpler path for her use case.
  Naming inconsistency surfaced: the BCN Activa audit repo is
  `Eticas-AI/Barcelona-Activa` on GitHub but referenced as
  `bcn-activa` everywhere else — cleanup opportunity, not urgent.

For the full chronological detail, see
[`Eticas-AI/leaflet/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/leaflet/blob/main/meta/INTERNAL_LOG.md).

## Strategy

- **Strategy workstream bootstrapped as a dedicated project
  (Mon May 11).** Initial commit on
  [`Eticas-AI/strategy`](https://github.com/Eticas-AI/strategy)
  (commit `e164dd3` on `main`, repo created today). Following the
  methodology bootstrap earlier in the morning, the strategy
  workstream was the next in line per the methodology
  `INTERNAL_LOG` migration roadmap. Repo initialised with the full
  layered meta-state (README, `meta/` with STATE/CONTEXT/
  INTERNAL_LOG/README, root TRACKER); three working artefacts
  migrated from the broader source project's PK to `docs/`:
  `business-strategy.md` (V1.0 Proposal by Noelia, April 2026),
  `competitive-landscape.md` (AI governance market analysis of 13
  competitors), `competitors.xlsx` (working competitor database
  with ecosystem-level defensibility analysis). Shape chosen:
  Career Scoops — PK empty, SharePoint Growth folder
  ([`eticastech.sharepoint.com/sites/Growth/`](https://eticastech.sharepoint.com/sites/Growth/))
  canonical for the broader, team-owned strategy assets
  (Positioning Document approved, Communication Strategy WIP).
  Scope made explicit in `meta/CONTEXT.md` and `TRACKER.md`:
  Eticas.ai commercial strategy (positioning, GTM, competitive
  intelligence, pricing, partnerships); research funding
  workstreams (NEOTEC, Eurostars, data space) remain out of scope
  and deferred to future bootstraps. Additional strategy-relevant
  material identified in a parallel Claude account
  (`Validation_of_Value_Proposition`, `eticas_competitive_analysis`,
  possibly `Eticas_Website_Copy_v3`) — migration deferred to a
  follow-up session in the new strategy project once uploaded.
  Gates Foundation files initially considered for strategy are
  actually Career Scoops deliverables and belong to
  `Eticas-AI/career-scoops-audit`.

- **First substantive session: scope narrowed, open commercial-model
  question recorded (Mon May 11).** Three commits to
  [`Eticas-AI/strategy`](https://github.com/Eticas-AI/strategy)
  (`ab40c99`, `beddf21`, `46199c2` on `main`). First contact with
  reality after the morning bootstrap revised the repo's role: rather
  than hold version-controlled copies of Noelia's strategy documents
  (which iterate in SharePoint at a cadence that makes repo copies
  stale within days), `docs/business-strategy.md` was reduced to an
  annotated stub mapping the live SharePoint material (Business
  Strategy 2026, Validation of Value Proposition, HR Messaging,
  Literature support, Competitor list), `docs/competitors.xlsx`
  removed, and the repo's role re-framed as a team thinking space
  for decisions, repo-side analyses, drafts and exploratory work —
  not a mirror of Noelia's workspace. The `[TBC]` placeholder for
  `Current focus` in STATE.md was resolved to two threads: Noelia's
  Validation of Value Proposition cycle, and a newly-opened question
  on commercial-model direction (potential shift from audit-led
  delivery toward continuous-monitoring delivery, most realistically
  in partnership with observability providers rather than by building
  infrastructure). The latter was prompted by Gemma's 8 May
  "Learning from AI observability" thread and the prior 29 April
  "Product roadmap" thread, with the operational ceiling on blind
  external LLM audits (Wiselook is the live example) as background.
  Registered in TRACKER as an explicit open question, not yet a
  decision — to be discussed in the team meeting later today.
  Reply drafted to Gemma and Noelia acknowledging the diagnosis,
  endorsing Noelia's "complement observability providers" framing
  as the realistic step given current build capacity, and deferring
  detailed architectural decisions.

For the full chronological detail, see
[`Eticas-AI/strategy/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/strategy/blob/main/meta/INTERNAL_LOG.md).

## AI risk taxonomy

- **Repo ordering pass: canonical version source formalised (Tue May 12).**
  One PR ([`Eticas-AI/ai-risk-taxonomy#18`](https://github.com/Eticas-AI/ai-risk-taxonomy/pull/18),
  branch `repo-ordering`, merged commit `0992c4b`) addressing accumulated
  staleness in the human-facing docs plus the durable fix. Three docs
  refreshed to v1.2.0 state: `README.md` (version 1.0.0 → 1.2.0, framework
  list rewritten to 14 grouped by bucket, Agentic AI added as established),
  `TAXONOMY.md` (Agentic AI added to client-perspective table and Risk
  categories list using the canonical definition; alignment-with-external-
  frameworks table refreshed; wrong site URL `eticas-ai.github.io` replaced
  with `taxonomy.eticas.ai`), `docs/implementation.md` (stale `scheme:` block
  removed from `taxonomy.yaml` example). Durable fix: `build/check_yaml.py`
  extended with `validate_version_consistency()` that fails CI if `README.md`
  or `TAXONOMY.md` carry a `**Version:** X.Y.Z` differing from
  `src/config.yaml` — formalises `src/config.yaml` as the single source of
  truth. Also housekept: stale `nist-600-completeness` branch deleted from
  origin (PR #17 squash-merge had captured the content; branch was a
  leftover). Pendientes file in project knowledge identified as fully stale;
  small instruction snippet drafted for the project block. Full chronological
  detail in [`Eticas-AI/ai-risk-taxonomy/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-risk-taxonomy/blob/main/meta/INTERNAL_LOG.md).

- **External-review triage, follow-ups register, and alignment-doc
  refresh (Wed May 13).** Gemma forwarded the output of a separate
  Claude session reviewing the public taxonomy site. Triage against
  the canonical YAML surfaced one genuine precision issue
  (Transparency / NIST 600-1 cell marked `close — #8` while the YAML
  had `relation: relatedMatch`); six items already current in the
  repo but not visible from the public surface; and two misframings
  of by-design choices (Organisational Readiness described as
  "removed", Agentic AI described as a rename of Autonomy & Agency).
  The low signal-to-noise traced to the reviewing session having
  access only to the public site — by design a curated subset of
  the canonical material.

  [PR #19](https://github.com/Eticas-AI/ai-risk-taxonomy/pull/19)
  (squash-merge
  [`9a5c5ab`](https://github.com/Eticas-AI/ai-risk-taxonomy/commit/9a5c5ab))
  applied three precision patches — the Transparency citation added
  in YAML with the alignment-doc cell updated to
  `related — via #8 (indirect)`; two stale `alt_labels` removed
  (`'Reliability & Manipulation'` on `reliability`,
  `'Agentic Risks'` on `autonomy-agency`, both pre-v0.3 / pre-v1.1
  leftovers surfaced during triage rather than raised by the
  reviewer — `alt_labels` don't render publicly) — and introduced
  `docs/external-review-followups.md` as a register pattern: items
  by status (pending / resolved / by-design) with an explicit
  Source-of-truth section explaining the public/internal split as
  a deliberate publication-strategy choice, so future review
  sessions have a structural entry point rather than relying on
  briefings.

  Same-day follow-up direct to `main` (docs/-only) as commit
  [`6867782`](https://github.com/Eticas-AI/ai-risk-taxonomy/commit/6867782):
  refresh of `docs/external-framework-alignment.md` from v0.3 / 10
  frameworks to v1.2 / 14 frameworks (CoE CETS 225 and TC260 as
  v1.0 additions, CSA Agentic Profile and OWASP Agentic Top 10 as
  v1.2; per-framework Notes paragraphs; new "Agentic AI as a
  standalone category" sub-section; "Autonomy & Agency dual
  coverage" rewritten to reflect the v1.1 split; granularity counts
  9/18/70 → 10/20/77). Main alignment table preserved at the
  original 9-category × 10-framework cross-walk — the new
  frameworks are scoped narrower and read more clearly in narrative
  form than as additional columns. Tonal pass on the follow-ups
  page itself in the same commit reframed the public/internal
  split as "two complementary views" rather than "filtered view,
  not the source" (Gemma will publish the public site in her
  paper, so the framing matters); the by-design citations item
  clarified to state that the research record is available for
  paper use via the verification doc and YAML citations — the
  public site lightness is a publication choice, not a structural
  commitment.

  No version bump (everything docs/ + meta/). Email to Gemma and
  Alex drafted with the follow-ups page link; Pablo sent
  manually. Net for the session: one real precision fix from
  external review, plus the structural register and the alignment-
  doc refresh that the next reviewer (or Gemma's paper work)
  needed anyway. Full chronological detail in
  [`Eticas-AI/ai-risk-taxonomy/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-risk-taxonomy/blob/main/meta/INTERNAL_LOG.md).

## ACHILLES

- **Migration to canonical pattern: bootstrap of
  [`Eticas-AI/achilles`](https://github.com/Eticas-AI/achilles) (Tue May 12).**
  Repo created today, populated with 22 files in commit
  [`8681839`](https://github.com/Eticas-AI/achilles/commit/8681839)
  on `main`. Prior state: a Claude project on Pablo's personal account
  with the ACHILLES corpus and rich memory; substantive history lived
  entirely there, no dedicated repo. Shape chosen: **DataPACT (hybrid)** —
  PK kept for the operationally-active subset of submitted M18
  deliverables (D2.1, D2.3, D4.1, D4.4, D7.1, D9.4) + Grant Agreement;
  repo `docs/deliverables/` indexes all 13 submitted M18 deliverables
  with one-page summaries; SharePoint canonical for everything official.
  Fact-sheet workflow (Eticas-authored markdown + Node.js
  `factsheet_core.js` generator) lifted out of PK into
  `docs/assets/factsheets/` and `tools/factsheets/`, with READMEs
  installed at bootstrap and content to be moved by Pablo in a
  follow-up commit. §3.1 PK cache pattern not activated, consistent
  with the 9 May reversion to opt-in. Inventory in `ai-ops` updated
  (commit
  [`b54704f`](https://github.com/Eticas-AI/ai-ops/commit/b54704f)).
  PAT-backed bootstrap (single push of 22 files); same PAT used for
  the `ai-ops` inventory update.

- **Migration lessons surfaced — multi-tenant SharePoint and
  financial-admin handoff (Tue May 12).** Two refinements to the
  runbook identified during this migration, logged in the project's
  `meta/INTERNAL_LOG.md` and not yet folded back into
  `Eticas-AI/ai-ops/migration-runbook.md`. (1) The Phase 2d reading
  paths and the Phase 5 step 4 "Project-declared additional sources"
  wording currently assume the project's canonical SharePoint sits
  on the same tenant the M365 MCP authenticates against. ACHILLES
  violates this: the canonical SharePoint is on Fraunhofer Portugal,
  Pablo is a guest; reading from the canonical store requires the
  browser, PK upload, or chat upload — direct MCP reads fall back to
  Eticas SharePoint mirror plus repo summaries. EU consortium
  projects routinely have this shape (coordinator's tenant
  canonical, partners are guests). Worth documenting as a first-class
  pattern. (2) Financial-admin handoff (PM tables, Financial Statement
  spreadsheet) as a first-class STATE.md item: WP/task lead drafts
  technical content, a separate financial admin owns the numbers.
  Currently mixed in the same to-do list; would benefit from a
  cleaner ownership-split convention. Both folded into the next
  runbook pass when the same patterns recur on another project (or
  earlier if it makes sense).

- **Current focus surfaced on bootstrap: RP1 M18 reporting
  (Tue May 12).** Francisco Juliá's email earlier today (PNO,
  financial reporting lead) opened RP1: technical report at
  Fraunhofer SharePoint
  `WP9 - Project Management/4-Periorid Report M18/`, financial
  statement Excel attached, internal deadline 2026-05-15 (15-day
  draft to PO by 2026-06-02, FS portal upload by 2026-06-05,
  review meeting 2026-06-17 remote, EC submission 2026-06-30).
  Pablo owns WP2 technical content (Section 1.2.2 — already
  drafted, partner inputs consolidated, summary refinement
  pending) plus Section 1.3 Impact, Section 3 DMP update, Section 5
  Deviations, and the T4.3/T4.5 task-leader contributions under
  WP4. Financial Statement Excel and the WP2 PM table handed off
  to Cristina [TBC: surname/role]; Pablo to leave a comment in the
  Excel pointing her to the template and the deadline. Six action
  items recorded in `TRACKER.md` (AI-001..AI-006), full deadline
  cascade in `STATE.md`.

- **M18 PTR Eticas content drafted (Tue May 12).** Same-day
  follow-up to the morning's bootstrap. Drafted in markdown all
  Eticas-owned sections of the Periodic Technical Report:
  Section 1.2.2 (WP2 — summary plus T2.1, T2.2, T2.3 task
  descriptions), Section 1.2.4 (T4.3 and T4.5 task descriptions),
  Section 1.3 (Impact), Section 3 (DMP update). Section 5
  Deviations kept N/A, consistent with M12. The M18 draft inherits
  literally from M12 (byte-for-byte copy-paste across all WP
  sections), so the work was extending the M12 narrative to the
  full M1–M18 span and reframing temporally. Two emails composed
  for sending after SharePoint link fill-in: WP2 partner
  validation chase (deadline Mon 18 May) and an internal
  verification request to Mariana for any pre-handover Eticas
  work on T4.3 / T4.5 (M1–M15). Two fact-sheet README corrections
  also landed earlier in the day
  ([`042e61e`](https://github.com/Eticas-AI/achilles/commit/042e61e),
  [`e6809f1`](https://github.com/Eticas-AI/achilles/commit/e6809f1)):
  governance clarified (each partner owns its own fact sheets;
  Darlene at CUOMOIT coordinates the catalogue) and two
  KUL-authored fact sheets re-attributed. PK cleanup and migration
  of useful loose docs to the repo deferred to next session.

For the full chronological detail, see
[`Eticas-AI/achilles/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/achilles/blob/main/meta/INTERNAL_LOG.md).
