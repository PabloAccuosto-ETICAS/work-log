# Week 2026-W21 (May 18 – May 24, 2026)

## AI risk taxonomy

- **Cloudflare Access extended to `/dist-internal/*` (Mon May 18).**
  Operational follow-up to v1.4.0. On the Cloudflare dashboard,
  `taxonomy.eticas.ai/dist-internal/*` was added as a public hostname
  to the existing self-hosted Access application that already protects
  `/risk-internal/*` — single application, multiple hostnames,
  inheriting the Azure AD identity provider and `Allow @eticas.ai`
  policy. No new application, no new policy. Verified by `curl -I`
  returning 302 to `eticas-ai.cloudflareaccess.com` on both
  `taxonomy.ttl` and `taxonomy.jsonld` under `/dist-internal/`.
  Closes the access part of the inconsistency surfaced before
  v1.4.0; v1.4.0 had already closed the licence-claim part. Meta
  updates committed direct to `main`
  ([`bb655c2`](https://github.com/Eticas-AI/ai-risk-taxonomy/commit/bb655c2)).

- **v1.5.0: four IEEE standards added as external frameworks
  (Mon May 18).** Triggered by Alex's email of 2026-05-15
  (Pablo + Gemma cc). New frameworks: IEEE 7001-2021 (Transparency
  of Autonomous Systems, `compliance`), IEEE 7002-2022 (Data Privacy
  Process, `compliance`), IEEE 7003-2024 (Algorithmic Bias
  Considerations, `compliance`), IEEE 2894-2024 (Architectural
  Framework for Explainable AI, `reference` — IEEE Guide, advisory).
  Four category-level mappings on `bias-fairness`,
  `privacy-confidentiality`, `transparency-explainability` (×2).
  Counts unchanged (10/20/77); URIs stable; SKOS triples public
  517 → 525, internal 1736 → 1744 (+8 each, exactly 2 per new
  mapping). The 700x series went to `compliance` because each
  self-describes as conformity-assessable in its own text; 2894 went
  to `reference` because IEEE publishes it as a Guide, which is
  advisory rather than normative. All four mapped at category level
  only — the standards describe process methodologies cutting across
  whole categories (CoE CETS 225 / TC260 pattern). The
  IEEE 7003 mapping's `target_id` is `clause-7-and-9` matching the
  loci Alex highlighted; the citation broadens beyond her two
  sub-group illustrations to also acknowledge representational harm.
  Terminology fix on encoding: "Article" → "Clause" (IEEE's own
  vocabulary). [PR #22](https://github.com/Eticas-AI/ai-risk-taxonomy/pull/22),
  tag [`v1.5.0`](https://github.com/Eticas-AI/ai-risk-taxonomy/releases/tag/v1.5.0),
  merge [`c1f3bf7`](https://github.com/Eticas-AI/ai-risk-taxonomy/commit/c1f3bf7).
  Same-day cleanup
  ([PR #23](https://github.com/Eticas-AI/ai-risk-taxonomy/pull/23),
  merge [`866cd2c`](https://github.com/Eticas-AI/ai-risk-taxonomy/commit/866cd2c))
  caught during deploy verification: the citation example on
  `/about/` was hardcoded to `v1.4.0` and missed by the version bump.
  Fixed the literal and extended `build/check_yaml.py` so future
  bumps fail CI if `about.md` drifts. The `v1.5.0` tag points at
  `866cd2c` so the tagged commit is internally consistent.
  Reply email to Alex (Gemma cc) sent confirming deploy. Full
  chronological detail in
  [`Eticas-AI/ai-risk-taxonomy/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-risk-taxonomy/blob/main/meta/INTERNAL_LOG.md).


## Methodology Phase 4 closed end-to-end

Single working day closed Phase 4 of the audit-findings architecture work (planned 2026-05-15 via ADR-0004; executed today). Six substantive commits across two repos, repo bumped from v0.3.8 → v0.4.0.

**`Eticas-AI/ai-audit-methodology`** (five commits + housekeeping):
- `ba889de` — Batch 1: L2 entry for pii-leakage + L3 stub directory + ADR-0004 sketch correction.
- `99ad26a` — Batch 1 follow-up: metric.range unification.
- `b188d9b` — Batch 2: `src/eticas_audit_methodology/` Python package (Pydantic models, aggregation engine split, loader); 32 tests passing.
- `437108e` — Batch 3: migrate pii-leakage example to multi-file audit-findings + L2 application with provenance.
- `b495e30` — Batch 4 methodology side: migrate metrics_registry from leaflet + ADM stage aggregation open question documented.
- `846b005` + `ad7bbbf` — Batch 5: post-execution close-out (inventory completion + STATE/TRACKER/CHANGELOG to v0.4.0).

**`Eticas-AI/leaflet`**:
- `2a3a0d3` — Cross-repo cut-over: consume `eticas-audit-methodology` as dep; rewrite render+cli for multi-file input; v0.1.0 → v0.2.0; wiselook → `legacy/`.

**Decisions of note** (more detail in methodology repo's INTERNAL_LOG 2026-05-18 entry):
- Python package name = `eticas_audit_methodology` (dist `eticas-audit-methodology`).
- Cross-repo dep mechanism: editable install local + Git URL pinned in CI.
- Wiselook flat-model fixture → `Eticas-AI/leaflet/legacy/`; re-implementation deferred.
- ADM stage aggregation: flat-model rule (untested `[TBC]`) deleted rather than mechanically ported. 4 design options documented in `aggregation-model.md`; natural validator = BCN Activa.
- DecodingTrust leaflet HTMLs commit to leaflet repo's `examples/` (existing convention, hooks into Netlify).

**Outstanding for next sessions** (handovers produced):
- Methodology project: documentation/schema review pass post-Phase-4.
- Leaflet project: docs alignment + generate DecodingTrust pii-leakage leaflet HTML.

**Tool note.** Local terminal lost partway through Batch 5; switched to GitHub MCP. Functional end-state identical; Batch 5 ended up as 2 commits instead of 1 atomic due to payload size.

## Methodology post-Phase-4 documentation reconciliation pass

Evening session same day (Mon May 18) following morning's Phase 4 closure. Structural / coherence read-through of all live docs in `Eticas-AI/ai-audit-methodology` catching stale pre-Phase-4 framing. 11 substantive commits direct to `main`; no version bump (stays v0.4.0); tests 32/32 throughout. Two structural decisions banked.

**Documentation reconciliation** (~10 commits). ADRs 0001/0002/0003/0004 received post-Phase-4 implementation annotations (parallel to ADR-0001's existing 2026-05-15 pattern); Layer 1/2/3 READMEs and `glossary.md` refreshed for paths/tense/hierarchical-model status (~10 glossary entries + §"Hierarchical model" section rewrite); `docs/architecture/audit-findings-architecture.md` and `aggregation-model.md` past-tense for Phase 3 + Phase 4 milestones; `meta/CONTEXT.md` and `testing/datasets-benchmarks.md` for cut-over framing; final cross-cutting sweep on the few stragglers (`layers/layer-1-foundations/leaflet/README.md` was missed in the first pass).

**Pattern A.2 ratified + implemented** (commit `d15f79c`). Mid-session decision: each subdirectory under `examples/decodingtrust/` is per-dimension (not per-subcategory). Triggered `git mv examples/decodingtrust/pii-leakage/` → `examples/decodingtrust/privacy-confidentiality/` cascading through ~10 doc punteros + the `audit_id` field. `bias-fairness/` (next subdir to author) will hold 3 subcategories within one audit findings — exercises dimensional breadth-of-concern.

**L2 hierarchical directory layout decision banked** (implementation deferred to next session). Late-session, Pablo's review of `layers/layer-2-core/llm/` surfaced four issues: `methodology-review.md` is WIP/outdated and at level-mismatch; `fairness.md` + `reliability.md` are exploratory but physically next to canonical `pii-leakage.yaml`; the directory layout question opens; the general README is poor + outdated. Decision: L2 directory moves from flat (`<subcategory>.yaml` per ADR-0004 §1 as written) to hierarchical, mirroring Pattern A.2 at L4 (`<dimension>/<subcategory>.yaml`). Rationale (Pablo's wording): "facilidad para humanos y simetría con implementaciones a nivel layer 4". 2-nivel vs 3-nivel sub-decision left open for next-session ratification (recommendation: 2-nivel).

**Comprehensive next-session handover artifact** committed ([`e640478`](https://github.com/Eticas-AI/ai-audit-methodology/commit/e640478) → [`meta/handover-llm-core-cleanup.md`](https://github.com/Eticas-AI/ai-audit-methodology/blob/main/meta/handover-llm-core-cleanup.md)). ~450 lines. Bundles two coordinated workstreams (hierarchical L2 implementation + LLM core cleanup) into a single next-session brief; 5 workstreams (A: hierarchical L2 restructure / B: methodology-review.md out of L2 / C: fairness/reliability to `_background/` / D: rewrite LLM core README / E: ADM core README parallel rewrite); exhaustive per-file checklist (~13 edits across 12 files + 4 moves + 2 new files). Analysis pre-done so next-session execution doesn't require re-scoping.

**Closeout deltas** (this entry + companion entries in methodology repo's TRACKER `499f6b4` + CHANGELOG `499f6b4`; STATE.md and INTERNAL_LOG.md were updated during execution): close the post-Phase-4 documentation reconciliation pass formally. Full chronological detail in [`Eticas-AI/ai-audit-methodology/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-audit-methodology/blob/main/meta/INTERNAL_LOG.md) (2026-05-18 evening entry).

**Tool note.** Local terminal worked for commits 1–10; switched to GitHub MCP for the handover artifact + closeout deltas (commits 11+). Functional end-state identical.

- **2026-05-19 (two sessions): repo-wide structural cleanup landed without version bump.** First session executed `meta/handover-llm-core-cleanup.md` end-to-end (8 commits): L2 hierarchical layout `<dimension>/<subcategory>.yaml` mirroring Pattern A.2 at L4 (2-nivel ratified at session start); `methodology-review.md` moved to `docs/reviews/`; `fairness.md` + `reliability.md` moved to `_background/`; LLM + ADM core READMEs rewritten in parallel; `_background/` and `docs/reviews/` READMEs created. Continuation session same day (8 more commits): Option C mechanism-discriminator inside L2 slots (`test_approach`, `metrics`, `default_severity_bands`, `benchmarks` items carry a `mechanism:` field; permits top-down implementation independent of available benchmarks; surfaces structural gaps as `probes: []`); L4 dual-child layout (`layer-4-projects/` for engagement-audit pointers + new `layer-4-validations/` for methodology-validation audit content); `examples/decodingtrust/` moved to `layer-4-validations/decodingtrust/`; buffers moved from `meta/` to new top-level `catalogs/` directory. ADR-0004 §1/§2/§4 annotations extended; glossary entry 40 updated. Tests 32/32 throughout.

- **2026-05-20: W1 closed + implementation-forward principle landed in `ai-ops` (PR #18) + audit-depth introduced as Layer 1 methodology axis.** W1 in commit `90bbe6d`: pii-leakage memorisation route operationalised end-to-end (2 probes by cue-type — `prefix_priming_extraction` + `attribute_query_extraction`; `pii_extraction_rate` metric; stricter memorisation severity bands sev-5-at-30% vs disclosure's 60%; DecodingTrust §8.1 Scenario 1 `probe_mapping` populated and verified against Tables 24-25; `data_requirements` discriminated by mechanism). Implementation-forward principle adopted in `Eticas-AI/ai-ops` ([PR #18](https://github.com/Eticas-AI/ai-ops/pull/18)) distinguishing senior-review-as-precondition (anti-pattern) from senior-input-as-next-iteration (default); `status: exploratory` means in-use-with-known-open-questions, not blocked-on-validation. Methodology repo swept against the new principle in commit `6b1fa77` (STATE / pii-leakage.yaml header + DRAFT band notes / Usman taxonomy-merge framing reframed; superseded handover deleted).

- **2026-05-20: audit-depth designed as Layer 1 methodology axis** (concept doc + ADR-0005 + glossary 43-45 + 2 handovers). Two-axis model (lifecycle layer × access mode) promoting the concept from narrative-only (leaflet rationale v4 Appendix; client-report philosophy "limitations are findings"; BCN Activa) to structured L1 component with schema hooks for L2 (`requires_depth` on slot items) and L4 (`audit_depth` on `AuditMetadata`); `DimensionCoverage.not_assessed` refined with reason codes. Implementation deferred to next session per `meta/handover-access-depth-implementation.md` (~90-120 min, backward-compatible); W2 bias-fairness reissued with audit-depth integrated per `meta/handover-bias-fairness.md`, sequenced after. No version bump. Honest note: closeout exposed an MCP-payload-pressure mistake that mutilated INTERNAL_LOG temporarily (caught + restored manually); pattern correction documented — avoid MCP for surgical edits inside large append-only files.

## Eticas.ai commercial strategy

Follow-up to the open decision of 2026-05-11 on shifting toward continuous-monitoring delivery. Two recent position papers brought by Gemma (Kirgis/Narayanan et al. on log analysis; Pacchiardi et al. on CL-Eval) were analysed in chat and synthesised into a layered frame: build log analysis on partner observability data first (layer 1); extend with trajectory-aware evaluation for systems with persistent memory or post-deployment learning later (layer 2). Reply sent to Gemma on team chat; framing committed as TRACKER follow-up entry in [`Eticas-AI/strategy`](https://github.com/Eticas-AI/strategy/commit/71f376f).

## Wiselook audit

- **LangGraph STG unblocked (Tue May 19).** Jaime's email of
  2026-05-12 (Roberto endorsing, Borja and Noelia cc) confirmed
  the STG deployment URL of Claire on LangGraph Cloud and that the
  existing `WISELOOK_LANGGRAPH_API_KEY` authenticates against both
  PROD and STG. Both questions of the 2026-05-11 email closed in a
  single exchange. End-to-end STG path now exercisable: partner
  API (working since 2026-05-11) + LangGraph STG can be driven from
  the same orchestrator with no further external dependency. Sole
  remaining Wiselook-side pending: 2026-05-08 email asking for
  functional equivalence STG ↔ PROD (model versions, prompts,
  rubric, scoring logic) — parts of which may now be empirically
  verifiable given both environments are exercisable.

- **Admin portal errors of 29/4 paused by audit decision (Tue May
  19).** Wiselook is working on `/campaigns`, `/archetypes` and the
  WiseSearch list/search failures; not central to audit scope.
  Removed from "Blocking on Wiselook" and "Pending email to
  Roberto" in `meta/STATE.md`. Re-escalate only if they recur in a
  way that blocks audit work.

- **Meta updates.** Both deltas applied as small surgical edits to
  `meta/` via the GitHub MCP. `meta/STATE.md`
  ([`1e5f65e5`](https://github.com/Eticas-AI/wiselook-audit/commit/1e5f65e53532a8146c244aa1547685e587058168))
  rewrote TL;DR (no external blockers active), emptied "Blocking on
  Wiselook", reframed the orchestrator item from "parameterisable
  skeleton" to "implement and validate end-to-end on STG", and
  updated the Key facts entry on LangGraph with the STG URL + same-
  key confirmation. `meta/INTERNAL_LOG.md`
  ([`94eb2a6c`](https://github.com/Eticas-AI/wiselook-audit/commit/94eb2a6cb14d94097f46aa5a4acc4148a5c769d4))
  added two new rows: the 12/5 receipt and the 19/5 pause decision.
  Next actionable step: orchestrator implementation
  (`partner_api.py` + LangGraph client refactor to take
  `(base_url, api_key)` parametrically; add
  `WISELOOK_LANGGRAPH_STG_BASE_URL` to `.env.example`).


## DataPACT

- **Role reframing by Gemma + fourth partner check-in (Wed May 20).**
  Following the 2026-05-19 cross-project mail Pablo opened with
  Gemma and Mariana on EU-project bandwidth, Gemma instructed
  that Pablo should not take on regulatory work in DataPACT — his
  role is the technical contact for the project as a sandbox for
  Eticas's broader offer; T1.4, EAB, and partner alignment on
  GDPR / AI Act / ethics fall to Mariana, Alex and Gemma.
  AIGENT4CYBER closed off Pablo's plate same day by Mariana (she
  covers the 9-10 June Burgos plenary remotely). Today,
  independently, Alexandra Papageorgiou (KU Leuven, WP4 lead)
  wrote checking on Pablo's absence from WP4 biweekly calls "for
  the past couple of months", with Gemma in cc — a fourth
  partner-visibility datapoint after Anja, Soudabeh, and the
  pattern Pablo flagged internally on 2026-05-19. Reply sent
  same day extending the cc chain to Anja, Mariana and Alex
  Magaard; covered WP4 calls + T1.4 + management meetings in one
  message; committed concrete next steps "in early June" without
  substantive commitments beyond timeline. Alexandra acknowledged
  the reply briefly; thread closed pending the post-Gemma-return
  conversation (28 May onward).

- **State updates** ([`f1d30ca`](https://github.com/Eticas-AI/datapact/commit/f1d30ca)).
  Full chronology of 2026-05-19/20 logged in
  [`Eticas-AI/datapact/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/datapact/blob/main/meta/INTERNAL_LOG.md)
  with verbatim of Gemma's role-reframing instruction.
  `meta/STATE.md` rewritten: TL;DR captures the reframing; new
  "What is on hold during the Gemma-out window (21–28 May)"
  section enumerates the external regulatory items paused until
  the reparto is agreed; Blockers reorganised around the reparto
  rather than around the previous mapping-doc dependency;
  preparation document for the conversation with Gemma added to
  Current focus as the key deliverable of the half-time window
  (Pablo travels to Uruguay 22 May, half-time through 4 June,
  fully offline 25 May and 4 June).
