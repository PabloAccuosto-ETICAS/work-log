# Week 2026-W26 (Jun 22 – Jun 28, 2026)

## Use cases

- **2026-06-22: stood up the use-cases publishing pipeline end-to-end — Notion → `projects.json` → Cloudflare Pages, live.** First implementation session in the dedicated project (internal tooling that publishes Eticas use-case pages from Notion). Took the index.html Noelia generated from Notion and turned it into a deployable static site: moved the inlined `const DATA` out of `app.js` into `data/projects.json` (fetched at runtime), placed the site source under `src/`, and dropped a duplicate ACCO record (PR #1). Reframed the architecture to a client-side render model — the generator's only job is `Notion → projects.json`, the static shell is copied to `dist/` (CONTEXT updated). Built `scripts/generate.js` (reads the "Project Case Studies" DB via a dedicated read-only Notion integration, Approved records only, filtered server-side) and `.github/workflows/deploy.yml` (`workflow_dispatch`: build + Wrangler Direct Upload to project `eticas-use-cases`); validated the generated JSON against Noelia's snapshot field-by-field (PR #2). Pablo created the Pages project, set the Actions secrets, and ran the workflow — site live at `eticas-use-cases.pages.dev` and repeatable. Follow-ups: Cloudflare Access (OTP + email allowlist) in front of the site, chosen over a shared-password middleware; one empty Approved row to tidy in Notion; repo visibility still open. Detail in [`Eticas-AI/eticas-use-cases/meta/log/2026-06-22.md`](https://github.com/Eticas-AI/eticas-use-cases/blob/main/meta/log/2026-06-22.md) (session 2 entry).

## AI Audit Leaflet

- **2026-06-22: shipped a public, ungated "scorecard" variant of the audit leaflet plus a real verification QR, and made the landing index hand out a downloadable QR per leaflet.** Added a simplified, dimension-level public view (grade + plain-language severity label + a one-line basis + a per-dimension link into the public risk taxonomy; not-yet-assessed dimensions muted), print-to-PDF via `@media print`, and a real `segno` QR — replacing the hand-drawn placeholder — on both the full leaflet (linking to the public version) and the scorecard. The landing index now shows a larger public-scorecard QR on each card with one-click SVG/PNG download and an Open link. Merged via PRs [#13](https://github.com/Eticas-AI/leaflet/pull/13), [#14](https://github.com/Eticas-AI/leaflet/pull/14), [#15](https://github.com/Eticas-AI/leaflet/pull/15); renderer-only, 59/59 tests. Public URL scheme: `leaflet.eticas.ai/<slug>/<date>/public`.
- **2026-06-22: presentation pass over both leaflets — dimension taxonomy links, header rework, QR fixes.** Added taxonomy link icons on every dimension (Label + Coverage tabs of the full leaflet, and per-dimension on the public scorecard); moved each dimension's teaser to a line below the name (redundant `<Dimension> — <Grade>` prefix stripped); reworked both headers (VALID moved to a metadata-row chip, audit/validity dates relocated under the system sub-title, the full leaflet's header QR turned into an index-style "Public scorecard" block with an Open link); matched the public width to the full leaflet (880px) and removed the footer "Verify at…" line. Fixed a QR cropping bug (missing `viewBox`, so a CSS resize cropped instead of scaling) affecting the index and both leaflets. Merged via PR [#16](https://github.com/Eticas-AI/leaflet/pull/16); renderer-only, 59/59 tests.
- **2026-06-22: deferred a more informative public narrative to a methodology-side `public_summary` field (audit + dimension level) and drafted the proposal.** The public "Audit summary" and per-dimension lines currently project the audit/dimension `headline`, which is thin; a public density that explains the grade narratively (without naming subcategories/mechanisms) belongs in the findings per ADR-0002 (consumers project, they don't author). Prepared a draft proposal (field + authoring convention + draft DecodingTrust narratives for auditor review + the leaflet-side consumer wiring with fallback to the headline) for a methodology session. Pending infra (Pablo): a Cloudflare Access bypass for `*/public` so the public URL resolves ungated, and a decision on a stable per-system public alias (paths/DNS may still change); a CHANGELOG/version bump covering the public-scorecard feature set is also pending. Detail in [`Eticas-AI/leaflet/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/leaflet/blob/main/meta/INTERNAL_LOG.md) (2026-06-22 entry).

- **2026-06-23: implemented the public/gated split — two Cloudflare Pages projects/domains and a per-client URL slug.** Decided and recorded (TRACKER) a two-domain topology — `leaflet.eticas.ai` (gated, full leaflets) + `leaflet-public.eticas.ai` (ungated, public scorecards) — instead of path-based Access gating on one origin, so the confidential full leaflet is never written to the public origin (the build split *is* the boundary). Rewrote `build_site.py` to emit two output trees with per-client/system paths (`/<client>/<system>/<date>/` + a stable alias `/<client>/<system>/`, the QR encoding the public-domain alias), Eticas-only + per-client indexes, and a slug guard; migrated `deployments/*.yaml` from `slug` to `client`+`system`; added a second, variable-gated (`DEPLOY_PUBLIC`) Pages deploy to the workflow. Verified end-to-end (real findings fetch + render; scorecard QR decoded to the public alias). Merged via PR [#17](https://github.com/Eticas-AI/leaflet/pull/17), 59/59. Also worked out the Cloudflare Access model with Pablo (per-client path-scoped apps, no policy inheritance, `*.pages.dev` lockdown); `eticas-leaflet-public` project + DNS created and serving, `DEPLOY_PUBLIC` + Access apps still pending.
- **2026-06-23: "Audit" → "Evaluation" terminology and a coverage-as-note rendering pass on both leaflet variants.** Applied the Eticas terminology shift to user-facing copy across the public scorecard, the full leaflet, the landing index and the glossary (kept "Auditor" the role, the methodology's proper name, and the `audit-findings` filename). Changed both variants to render only assessed dimensions inline and summarise not-assessed ones in a single coverage note at the foot — coverage stays part of the result, less clutter — styled distinct from the footer disclaimer. On the full leaflet also: a "Download PDF" button (`window.print()`) replacing the near-invisible print tip, footer cleanup (dropped the confusing "Verify at …/<id>" lines and the stale placeholder), a print fix (orphaned "CHECKS" labels were dangling in the PDF), and tabs reordered to Label / Coverage / Raw data / Glossary. Public half merged ([#18](https://github.com/Eticas-AI/leaflet/pull/18)); full-leaflet half merged ([#19](https://github.com/Eticas-AI/leaflet/pull/19)) at session close. 59/59. Backlog raised: surface the client (`audit.client_organization`) on the leaflet — it exists in the schema but is never displayed (the shown `owner` is the system's maker, not the client).
- **2026-06-23 (adjacent): produced a handover to stand up a `private.eticas.ai` internal site.** Out of the Cloudflare access discussion, specced a Git-connected Cloudflare Pages project (team-default + per-path collaborator Access) to replace ad-hoc `eticas.pages.dev` zip uploads — with the access model, repo structure and an implementation checklist. Separate repo/project, to be created in a dedicated session.

- **2026-06-24: brand + copy pass on both leaflets and the index — eticas.ai wordmark logo, "Leaflet" tab, "Evaluation results".** Replaced the yellow "ETICAS" text box in the header with the negative (white) **eticas.ai wordmark** across both leaflet variants and the landing index; the PNG lives once under `templates/assets/` and is injected as a base64 data URI (Jinja global; `build_site.py` reads the same asset for the index), keeping every rendered HTML self-contained. Renamed the full-leaflet tab **"Label" → "Leaflet"** and the public scorecard section title **"Assessment results" → "Evaluation results"**; added `print-color-adjust:exact` on the header so the dark banner + white logo survive the PDF. A follow-up fixed the logo rendering stretched in the leaflet headers (a column-flex `align-items:stretch` was distorting the wordmark; `align-self:flex-start` restored its aspect ratio). Merged via PRs [#20](https://github.com/Eticas-AI/leaflet/pull/20), [#21](https://github.com/Eticas-AI/leaflet/pull/21); renderer-only, 59/59 tests. Pablo also set `DEPLOY_PUBLIC=true`, so the public deploy (`leaflet-public.eticas.ai`) is now live from CI; Cloudflare Access apps/groups and a CHANGELOG/version bump (now covering #13–#21) remain pending.

## Wiselook audit

- **2026-06-22–23: ran the full-traversal gender factorial (epoch B), discovered and diagnosed F-039 (silent under-scoring), and paused the fairness analysis pending Wiselook's reply.** Wiselook confirmed (18 Jun) the stable scoring model is deployed to STG+PROD (epoch B live) and that the agent prompt drifted gradually without dates → the pre/post is "before/after a bundle of changes." Verified the ECO deepening banks are byte-identical across the deploy (new `compare_eco_banks.py`), then ran the N=30/arm interleaved factorial (`gender-ft-20260622`). Surfaced a new system-side finding, **F-039**: ~5% of fully-completed evaluations (`completionPercent=100`, 5/5 ECOs driven) deliver a competency profile with only 4 of 5 scores, the global silently averaged over the subset — confirmed not a collection artifact (direct out-of-collector query of the public `last-evaluation` endpoint at +22 h still returns 4) and not drive-side (no desync, no conversation-length association); concentrated on PC/Autorregulación/Agilidad, never GI/AC. Tightened the rate with an overnight reliability probe (200 runs → pooled 13/256 ≈ 5.1%, 95% CI [3.0, 8.5]). Extended the analyzer with a `contrast-ft` subcommand (full-traversal: global primary + per-ECO Holm family) but held the canonical analysis: won't measure fairness on a system that silently drops a scored dimension while still changing. Sent Jaime a one-point query with a reproducible `curl` (mechanism? public endpoint under-exposing vs. internal?). Detail in [`Eticas-AI/wiselook-audit/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/INTERNAL_LOG.md) (session 9 entry) and [`research/findings.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/research/findings.md) (F-039).

- **2026-06-24: pivoted to operationalising the methodology against Wiselook — completed the gender score analysis (equivalence, F-040), locked the analysis dataset, and settled the artifact-organization scheme.** Decided not to keep stalling on F-039 (counterparty-side, pro-bono): extract results from the data on hand with disclaimers, and use Wiselook to put the refined methodology into practice, mirroring the DecodingTrust L4 pattern (Definition→Mechanism→Probe→Metric→Severity). Mapped Wiselook to the taxonomy (`bias-fairness` / `disparate-impact-protected-groups`) with three strands — `allocation-of-opportunity` (the score, done), `quality-of-service-disparity` + `stereotyping` (LLM-as-judge over Claire's generated reports, pending Strand 2). Showed the overnight reliability probe is the same experiment at N=100/arm (outcome-blind verifier: config identical, shared stimulus, complete-5 = 188) and **locked it as the primary dataset** (factorial N=30 = replication, pooled = sensitivity), declared before inspecting the contrast. Wrote `analyze_dbp.py` (real file at last; implements the §4.10.5 hierarchy) and ran it: **no practically significant gender disparity in the 5-ECO scores — equivalence established** (primary Δ=−0.004, MWU p=0.70, bootstrap CI [−0.025,+0.017] ⊂ ±0.5; all five per-ECO equivalence under Holm; concordant across delivered-global / pooled / the N=30 replication) → F-040. Decided to report the bootstrap CI (HL collapses under the modal-tie structure). Settled artifact homes: custom code + experiment records + confidential results pages in the engagement repo; the methodology package (schema/loader/aggregation) + L2 entries + a new `apply_bands()` (the banding gap) in the methodology repo; audit-findings YAML in `ai-audit-methodology/layers/layer-4-projects/wiselook-audit`; leaflet renders. Deferred deep reliability evaluation (system in development → provisional only). Detail in [`Eticas-AI/wiselook-audit/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/INTERNAL_LOG.md) (session 10) and [`research/findings.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/research/findings.md) (F-040).

- **2026-06-24 (2nd session): designed the Layer-4 results architecture for the gender result — settled how the audit findings, the internal results narrative, and the client report relate.** Grounded in the methodology schema (audit findings is the single source of truth per ADR-0002; consumers project, they don't author): report-bound narrative lives in the audit-findings YAML (multi-density text + evidence + recommendations), with no parallel store. Banked six decisions — a new `research/results/` layer (report-grade internal synthesis, sibling to `experiments/`, citing the YAML); a YAML-first build order (audit-findings → results page → an ADR documenting the layer-4-projects convention in the methodology); severity authored in the YAML (manual banding now, `apply_bands()` to automate); `allocation-of-opportunity` operationalised for Wiselook's continuous 1–5 score (severity 1 — no disparity); and `accessibility-barriers` scoped out (interface-level, while the engagement audits responses via the API) as a documented coverage note. No code or runs — design only. Detail in [`Eticas-AI/wiselook-audit/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/INTERNAL_LOG.md) (session 11).

- **2026-06-26: merged ADR-0007 (continuous-allocation metric) and the analyze_dbp.py consolidation, and cleared the LangGraph credential block.** In the methodology repo, shipped ADR-0007 (PR #12): a continuous-output allocation metric allocation_score_disparity (worst-unit |Δ| / practical-significance margin → dimensionless, universal severity bands), which relaxes one-metric-per-mechanism so a mechanism can carry multiple metrics: selected by applies_when.output_type; added apply_bands() (the banding gap from session 10); and re-expressed F-040 off its old L4 decision-rule override (categorical-rate-adapted) to the native continuous metric (metric_value 0.07 → severity 1, provenance L2, no override), reconciling the YAML evidence/narrative. 72/72 tests; honest scope flagged — apply_bands() has no production consumer yet (the findings linter is backlog). In the engagement repo, consolidated two divergent analyze_dbp.py (the stdlib analysis/ file and the numpy/scipy tests/tools/ file that produced F-040) into a single analysis/ implementation on one numpy/scipy stack, and reconciled the equivalence verdict from Hodges-Lehmann (which degenerates to [0,0] under the modal-tie structure, F-036/F-037) to the bootstrap CI on Δmean (PR #8); conclusion unchanged — F-040 reproduces exactly (Δ=−0.004, boot CI [−0.025,+0.017] ⊂ ±0.5, equivalence, 5/5 per-ECO concordant). Also resolved the Strand-3 blocker: the LangGraph 403 was a stale rotated key in the process env (Roberto reissued; .env reload → 200), not an auth/deployment change; confirmed the failing step authenticates solely via WISELOOK_LANGGRAPH_API_KEY (x-api-key), distinct from the Partner Bearer key, and that the key points to STG (keeps adverse content off PROD). Detail in Eticas-AI/wiselook-audit/meta/INTERNAL_LOG.md (session 14) and research/findings.md (F-040).

- **2026-06-26: ran the Strand-3 wiring smoke** — it confirmed the overlay wiring but surfaced a stochastic injection-coverage problem, producing four pre-launch decisions before the real N=50 blocks. Ran the N=2 mario/mario_sx smoke on STG (--out-dir tests/results/characterization): 4/4 traversal_ok, and both validation targets passed — the sexist overlay lands at the focal items and scores collect separably. Finding A (coverage): injection is qid-locked to AU3/GI3/AG3, and because Claire's deepening selection is adaptive/non-deterministic, the injection only fires when she happens to serve that exact item — across the smoke GI3 fired 4/4 (it is GI's first deepening), AU3 1/2, AG3 0/2 (AG often had no second deepening). So per-focal-ECO injection "dose" is uneven, and the real risk at scale is sub-contrast (a non-served focal makes the ECO identical across arms → null contrast), not over-divergence. The match_failure_rate=0.333 on mario_sx was one base-rate paraphrase (F-035), pre-scoring, self-healed by retry; not injection-caused (the failure is on Claire's question at AU, the first injection point, before any injected answer) and not a finding — the matcher stays strict. Established (and recorded the reasoning for) that a systematic arm-driven divergence in which questions get served is mechanically impossible for the first focal (Claire picks the question before seeing the injected reply; identical history → identical selection), and that what legitimately differs is the post-focal cascade → the contrast is a total effect (PC = pre-treatment placebo control, AC = post-treatment spillover). Captured four pre-launch decisions (item #1) with full justification in a handover/experiment record: (a) instrument per-run focal coverage (served ∩ {AU3,GI3,AG3}); (b) facet-widen the injection within AU/GI/AG to the items genuinely engaging SaberSer/SaberEstar (not just pos-3), keeping PC/AC and non-facet items clean; (c) instrument the served-path-by-arm (verify minimal pair held to the focal; quantify post-focal branching; also a Phase-2 Reliability signal, RQ-021); (d) a pre-registered inclusion rule — unit of analysis = the focal ECO (not the run), inclusion = focal-served (a pre-treatment event, so non-biasing, vs. the forbidden score-based rejection-sampling of decision 2b), require co-service in both arms for the paired contrast, and report effective N + discard rate per ECO. Real blocks (sexist then hostile, STG, tests/results/evidence) deferred until #1 is done. Detail in research/experiments/2026-06-26-strand3-coverage-and-inclusion-design.md and meta/INTERNAL_LOG.md (session 14).

## Eticas Platform

- **2026-06-26: finished the reference-architecture front/back coherence fix, rewrote STATE, reconciled ADR 0003 to prober-first, moved the deploy docs to the Netlify→Cloudflare migration, and captured the FullCircle kickoff.** Resumed the interrupted 25-Jun session: the reference-architecture artefact's auditor-surface ↔ run-orchestration pair was incoherent. Added the Scripts/CLI MVP surface (it had survived in the PK copy) and a `syncPair` auto-snap so only valid orchestration ↔ surface combinations are selectable (validated by JS syntax check + a transition simulation); committed `9e50c73`. Rewrote `meta/STATE.md` from scratch to the current state, reconciled ADR 0003's MVP preset from "C runner first" to **prober-first**, and updated `README`/STATE so deployment reads as a Netlify→Cloudflare Pages migration (`eticas.pages.dev`), mixed during the transition (commit `89f52f9`). Then captured the **FullCircle kickoff** (26 Jun): what they will send (effort estimates — a client-interface pilot: scorecard/leaflet from the YAML findings + a document-intake portal; a feature-rich version; the execution piece — plus a monthly hours report); their proposed approach (scorecard-first; YAML stays SSOT in git, docs/credentials to S3; JSON as an on-demand projection; client portal with auditor pre-validation; API-first; a dockerized execution unit that runs local/cloud/Eticas/client; hosting lean non-Microsoft); and the decisions/clarifications (audit lifecycle steps; "certification" → QA; grades live in the findings; client→project→document→version hierarchy; point-in-time immutable audits; private full leaflet vs a configurable public scorecard; execution-plane location as a parameter; LLM-first build priority). Annotated ADR 0003 with the dockerized-execution validation (also addresses the execution-cost concern Usman raised); committed `fd90c0c`. Action pending (Pablo): grant FullCircle access to the leaflet repo. Detail in [`Eticas-AI/ai-platform/meta/log/2026-06-26.md`](https://github.com/Eticas-AI/ai-platform/blob/main/meta/log/2026-06-26.md).
