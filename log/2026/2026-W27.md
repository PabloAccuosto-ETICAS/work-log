# Week 2026-W27 (Jun 29 – Jul 5, 2026)

## Wiselook audit

- **2026-06-29: closed out the deferred s12/s13 sessions and migrated the repo's meta layer to the per-day-log convention.** Regenerated `meta/STATE.md` from scratch, folding in the unreconciled s12 (25 Jun) and s13 (26 Jun) sessions: the Layer-4 layer is now recorded as built and merged (audit-findings YAML, synthesis page, ADR-0006/0007, `apply_bands`), and the gender-equivalence finding F-040 is re-expressed via the continuous allocation metric `allocation_score_disparity` = 0.07 → severity 1, grade A (provenance L2, no override). Migrated `meta/` to the per-day-log convention — froze the monolithic `INTERNAL_LOG.md` and root `TRACKER.md` (renamed `*-frozen-2026-06-29.md`, history preserved via `git mv`), established `meta/log/` with back-filled s12/s13 entries, rewrote `meta/README.md` and `CONTRIBUTING.md`, and reconciled broken doc pointers across README/docs. Merged via PR [#9](https://github.com/Eticas-AI/wiselook-audit/pull/9). Detail in [`Eticas-AI/wiselook-audit/meta/log/2026-06-29.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-06-29.md).
- **2026-06-29: cleared two bounded carry-overs (direct to `main`).** Committed the **Strand-3 coverage/inclusion design** as a dated experiment record (back-filled from the PK handover, so the temp handover can be retired) — the durable spec for Strand-3 item #1. Reconciled the **experimental-design doc to v0.8.2** so the gender-equivalence interval is reported as the **bootstrap CI on Δmean**, not Hodges–Lehmann (which degenerates to [0,0] under the modal-tie structure F-036/F-037); conclusion-neutral, aligning the design doc with the 26-Jun analyzer reconcile (PR #8) and the F-040 audit-findings YAML. Remaining Strand-3 work (item #1: runner instrumentation a–d + facet-widened stimulus authoring) deferred to a fresh session.
- **2026-06-29: began Strand-3 (seeded-adverse-stimuli) pre-launch item #1, on a feature branch.** Implemented and pushed the runner instrumentation — per-run focal-coverage (which injected items Claire actually served, since injection delivery is stochastic and qid-locked) and served-path-by-arm — additive only, unit-tested credential-free ([`strand3-prelaunch-item1`](https://github.com/Eticas-AI/wiselook-audit/tree/strand3-prelaunch-item1) @ `65a961e`). Derived and validated the facet-widened injection set (Autorregulación 3/4/7, Gestión de la Incertidumbre 3/5, Agilidad 3) from the rubric facet tags crossed with content-naturalness criteria. Remaining before launch: authoring the six adverse stimulus answers (sexist + hostile sets) and the pre-registered analysis inclusion rule; opens as a single PR when complete. Detail in [`meta/log/2026-06-29.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-06-29.md) (session 15).
- **2026-06-29: completed Strand-3 pre-launch item #1 and opened+merged it as PR [#10](https://github.com/Eticas-AI/wiselook-audit/pull/10) after a green staging validation.** Authored the six facet-widened adverse stimulus answers (Autorregulación 4/7, Gestión de la Incertidumbre 5; sexist + hostile sets) and the pre-registered analysis inclusion rule — locking the open methodological point as a **hybrid** (descriptive total-effect pattern as the primary frame, Autorregulación as the one clean causal anchor, a targeted single-ECO block held in reserve). Caught and corrected a dialect-consistency issue in the hostile stimuli (peninsular colloquialisms against a deliberately dialect-neutral baseline). A staging validation (N=3/arm, both arms) confirmed the instrumentation, the first clean run of the hostile arm, and that the widening gives Autorregulación reliable injection coverage; the real evidence blocks are now running on STG. Detail in [`meta/log/2026-06-29.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-06-29.md) (session 16).

- **2026-06-29: built the Strand-3 analyzer and produced a first (unregistered) read of the sexist evidence block.** Implemented, self-tested, and merged `analysis/analyze_strand3.py` (credential-free) — it applies the pre-registered inclusion rule (effective N + discard reported per focal competency) and computes the score-level three-way pattern: detection vs non-detection per directly-injected competency (with Autorregulación as the clean causal anchor, bootstrap-CI'd), and spillover onto the uninjected control competencies. The amplification (LLM-as-judge over Claire's narrative) and completion-toxicity legs are stubbed as deferred. Preliminary read of the collected sexist block (not yet registered as findings, pending the hostile block and the experiment record): Claire **does** register the injected sexist content in the scores (detection, not a construct-validity miss) and shows **no spillover** onto the control competencies; the open question — whether Claire *amplifies* the framing in the generated profile text — is the deferred leg. Detail in [`meta/log/2026-06-29.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-06-29.md) (session 17).
- **2026-06-29: hardened the methodology record for the eventual report.** Added a "Statistical treatment" subsection to the experimental design (§4.12, v0.8.3) capturing why Strand 3 is read as a descriptive pattern rather than a significance battery (the estimand is a within-run total effect; the per-competency score channel is bimodal/heavily-tied, so a per-competency significance test is mis-specified), the bootstrap CI on the causal anchor, and the deliberate non-import of the Strand-1 equivalence machinery — so the decisions need not be re-derived at report time. Separately, resolved a scores-payload subtlety: the per-competency `evaluations[]` array is the five competency records denormalised, not a facet breakdown (no retrieval error; the facet layer is internal and not exposed via the API) — already documented in the API notes, now also flagged in `STATE.md`.
- **2026-06-30: registered the Strand-3 score+coverage layer and turned it into a three-repo methodology contribution.** Ran the analyzer over **both** evidence blocks (sexist + hostile; reproduced bit-for-bit in a clean clone) and registered findings **F-041** (Claire detects the injected interpersonal content on the clean causal anchor and shows **no spillover** onto the control competencies, across two independent stimulus sets — the substantive result, since the focal penalty is an *expected* construct-validity positive control, not a bias finding), **F-042** (hostile candidate conduct elicits ~9× more off-bank questioning from Claire; fail-safe, so the analysed contrast stays clean) and **F-043** (one competency collapses to a zero-variance score under strong hostile injection), plus **RQ-028**. Detail in [`meta/log/2026-06-30.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-06-30.md) (session 18). The analysis surfaced a methodology contribution, now propagated: added two Layer-2 mechanisms to `stereotyping-demeaning-content` — `amplification` (the model reproducing/normalising demeaning framing the *subject* introduced) and `protected-attribute-insensitivity` (passively failing to register it) — as `ai-audit-methodology` PR [#13](https://github.com/Eticas-AI/ai-audit-methodology/pull/13), and prepared a new `emerging` taxonomy subcategory `measurement-validity` (the reliability counterpart, for when a non-detection concerns diffuse rather than protected-attribute content) for `ai-risk-taxonomy`. The taxonomy PR is prepared but not yet opened (tooling); full content + one-step instructions in [`meta/handover-2026-06-30.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/handover-2026-06-30.md). Core conceptual decision banked: **a non-detection's dimension is conditional on what is unregistered** — protected-attribute content is a bias/representational harm (disparate impact even if the insensitivity is uniform), diffuse content is a reliability/measurement-validity concern.
- **2026-06-30: reviewed the s18 Strand-3 methodology contribution, sharpened it, and integrated + merged it across the three repos (session 19).** A conceptual pass corrected the s18 framing — the "harm obtains even if the insensitivity is uniform" case is a **representational** harm (comparator-free), *not* disparate impact (that is the differential claim and needs a severity-matched comparator) — and split the bias non-detection into two mechanisms: reframed `protected-attribute-insensitivity` as the absolute/representational reading and added a new `protected-attribute-under-penalisation` (differential, `emerging`/DRAFT) in `ai-audit-methodology`, **merged** as PR [#13](https://github.com/Eticas-AI/ai-audit-methodology/pull/13). Opened + **merged** the `emerging` `measurement-validity` subcategory in `ai-risk-taxonomy` as PR [#26](https://github.com/Eticas-AI/ai-risk-taxonomy/pull/26) (kept under `[Unreleased]`, no version bump; flagged for the taxonomy team as a validity concept filed under the reliability dimension). Wrote the harm-dimension model — the representational/differential split, a **two-probe** stimulus design (oblique seed → subtle/absolute detection; overt **severity-matched within-type** pair → differential), and the concrete amplification manifestations in Claire — into experimental-design §4.12 (v0.8.4), and opened **RQ-029** (whether a protected-attribute-*triggered* spillover/halo should read as a bias rather than a reliability defect). Logged the remaining methodology refinement — an L4 "planned/in-progress" coverage state, chosen as a *separate field* rather than a `not_assessed` reason — as `ai-audit-methodology` issue [#14](https://github.com/Eticas-AI/ai-audit-methodology/issues/14). Verified the cross-repo coupling before merging (mechanism vocabulary is owned by L2; report generation does not load the taxonomy at runtime). Detail in [`meta/log/2026-06-30.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-06-30.md) (session 19).

- **2026-06-30: locked the Strand-3 amplification leg's design and pre-registered the judge rubric (session 20).** Settled the three design decisions — judge model (Claude Opus via API, temp 0, blind to the neutral/injected arm; with a documented self-judging caveat, since Claire's report agent is GPT-4o-class primary / Claude-Opus fallback and the served provider isn't yet extracted from the batch), external-API data handling (within existing project practice — synthetic persona, Wiselook aware), and unit of judgment (per focal competency section + executive summary, across both the employer- and candidate-facing reports). Pre-registered the **flag / ignore / amplify** rubric (worst-first sub-codes; null-baselined amplification rate against the methodology's `stereotyping-demeaning-content/amplification` bands) and **committed it before inspecting any injected report text**. Decided to build the judge harness in a fresh session rather than at the end of a loaded one (it is the highest-stakes instrument and the Strand-2 prototype). Separately fixed an imprecise Layer-2 prose definition — the `completion-toxicity` mechanism is identified by the *framing* axis (benign vs adversarial), with the seed (toxic/non-toxic) axis orthogonal — merged as `ai-audit-methodology` PR [#15](https://github.com/Eticas-AI/ai-audit-methodology/pull/15). Detail in [`meta/log/2026-06-30.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-06-30.md) (session 20).

- **2026-07-01: rebuilt the Strand-3 amplification judge as a per-channel harness, added a pluggable local-judge backend + feasibility gold set, and reconciled the meta layer (sessions 22–23).** Rebuilt the lost s21 branch from the handover spec — each competency section is now judged as two independent channels (`strength_channel`/`growth_channel`), one label each in a single judge pass, with a scope-based `subcode_mismatch` guard (the masking case: the same demeaning quote surfacing as both a strength endorsement and a growth-area flag, which the old single-label design collapsed); merged as PR [#14](https://github.com/Eticas-AI/wiselook-audit/pull/14). Added a pluggable judge backend (`--judge-provider {anthropic,openai_compatible}`, default local `qwen3:14b`) so a **local** model can be trialled for calibration without paid API access — arm-blindness and prompt text verified byte-identical across backends, and every local run is stamped `is_pre_registered_evidence_judge=false` — plus a 12-observation hand-labelled gold set and a PASS/FAIL probe design; merged as PR [#15](https://github.com/Eticas-AI/wiselook-audit/pull/15). Rebuilt the harness's progress/ETA reporting (PR [#16](https://github.com/Eticas-AI/wiselook-audit/pull/16)). Reconciled the meta layer that s21–s23 couldn't close: rewrote `meta/STATE.md` from scratch and back-filled the s21/s22/s23 day-logs (PR [#17](https://github.com/Eticas-AI/wiselook-audit/pull/17)). Core decision banked: **a local judge is for calibration/feasibility only; using it for an evidence run is a documented pre-registration amendment, not a flag flip.** Detail in [`meta/log/2026-07-01.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-07-01.md) (sessions 22–23).
- **2026-07-01: scored the local judge (PASS), built the amplification triage instrumentation, and kicked off a preliminary evidence pass under a documented amendment (session 24).** Built the agreement-scorer and scored `qwen3:14b` against the gold set → **PASS**: it reproduced the masking case (strength = amplify/A1 *and* growth = flag in the same section), matched every scored observation, and hallucinated no amplification — establishing the local model as a viable **triage/preliminary** judge. Then added the instrumentation the preliminary pass needs: per-observation judge **confidence** (harness v4) with two-tier agreement scoring by gold confidence; the **executive summary reported as a separate, informational stratum** (it compresses five competencies, so it paraphrases rather than reproduces the injected span — kept out of the clean section-level headline rate); text-echo of the evaluated text into every record; and a consolidated **human-review queue** in the analyzer that flags every observation needing manual review (all amplify/A1, sub-code mismatches, unjudged, and section↔summary cross-stratum inconsistency). A dated observation forced one design call: the local judge's **self-reported confidence is mis-calibrated** (it returns "high" on everything, including genuine borderline summaries the gold marked "medium"), so judge confidence is *recorded* but never triggers review — triage rests on the label-based criteria plus human judgement. Registered the **pre-registration amendment** (committed pre-data) authorising a **preliminary** amplification pass with the local judge on the sexist evidence block — driven by no Anthropic API access yet plus a due primary report/leaflets — with every resulting number labelled PRELIMINARY / non-pre-registered and the pre-registered Opus run explicitly **deferred, not cancelled**. Exercised the analyzer end-to-end on the wiring-smoke, then started the local-judge evidence run (left running at session close). Detail in [`meta/log/2026-07-01.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-07-01.md) (session 24).
- **2026-07-01: closed two open system-understanding questions from already-captured data, refreshed the methodology-coverage map, unblocked the hostile judge pass, and banked a phased plan to the first report (session 25).** Answered **RQ-009/RQ-010** with a new credential-free script (`analyze_reportdata_axes.py`) run against the already-committed gender-factorial batches (N=260 pooled): the candidate report's cultural "referente" per competency is a **fixed** template (confirmed F-015), while the "ejes transversales" are **dynamically synthesised** per candidate — re-opening the rationale-amplification concern F-014 flagged, now routed to the deferred Strand-2 leg. Refreshed `bias-fairness-operationalisation.md` to the 30 Jun mechanism split (now confirmed live in the canonical L2/taxonomy repos) and added a companion strand-organised coverage file, both newly linked from `STATE.md`. Found and fixed a silent gap in the hostile Strand-3 seed (missing `framing_description`/`injected_span` — the generic judge harness needed no code change, just the seed data) and extended the preliminary local-judge pre-registration amendment to cover the hostile block with its own dated note. Rewrote `STATE.md`'s next-steps as an explicit phased plan (close the running judge passes → first report on Strand 1+3, Strand 2 deferred → post-report refinements → new experiments) and banked the report-scope decision. Separately scoped the Strand-2 (María/Mario generative-output) harness design — architecture, in-scope fields, and a 4-dimension rubric — and built two exploratory pilots (not committed as harnesses): one testing the rubric on real examples, and one testing whether a local judge can guess candidate gender from `profile_summary` style alone. The second surfaced a real methodology lesson en route: after fixing an Ollama reasoning/token-budget bug, the judge's guesses turned out overwhelmingly one-sided regardless of the true label — a judge-calibration failure, not evidence about Claire's text — which redirected the priority toward a cheaper, deterministic lexical/semantic analysis on the full corpus instead. Full context for both pilots in a dedicated handover so the decisions don't need re-deriving. Detail in [`meta/log/2026-07-01.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-07-01.md) (session 25) and [`meta/handovers/2026-07-01-strand2-pilot-and-gender-guess-handover.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/handovers/2026-07-01-strand2-pilot-and-gender-guess-handover.md).
- **2026-07-02: recovered both Strand-3 evidence blocks, re-analysed them with corrected preliminary numbers, and diagnosed + fully specified the fix for a two-level defect in the insensitivity metric (session 26).** Diagnosed the cause of the "unjudged" cells (10 sexist / 70 hostile): the local qwen3 judge (a reasoning model) was exhausting its token budget on the `reasoning` field and returning empty `content` → `label=None` — not non-delivery (every unjudged cell had text present). Added retry-on-empty + a `--repair` mode to `judge_amplification.py` (re-judges only the null-label records of an existing artifact and merges them back, escalating `max_tokens` on retries while keeping the gold-validated reasoning-on config) ([`9fcc691`](https://github.com/Eticas-AI/wiselook-audit/commit/9fcc691)); Pablo ran it locally → **42 failures recovered, 0 remaining** (`…-repaired.json`, [`3aa6e7f`](https://github.com/Eticas-AI/wiselook-audit/commit/3aa6e7f)). Re-ran `analyze_strand3.py`: PRELIMINARY amplification (qwen3, section headline, employer_report) is **sexist sev 2** (0.023) and **hostile sev 3** (0.129), both concentrated in the strength channel (growth ≈ 0), executive summary clean (sev 1) — the recovered cells were not missing-at-random (the judge choked on the most demeaning content, so the pre-repair figure under-estimated). A question from Pablo ("how are we counting IGNORE?") surfaced a **two-level** defect in the insensitivity metric: the analyzer baselines the IGNORE rate against a ~1.0 neutral floor → clamps to severity-1 unconditionally (a floor artifact), and the deeper cause is that the canonical L2 YAML (`stereotyping-demeaning-content`) is internally contradictory — its formula and severity bands are absolute while its interpretation prose says "null-baselined" (a copy-paste from the amplification metric, where baselining is correct because the neutral floor ≈ 0). The corrected reading (absolute, delivery-conditioned, section-level) is **sexist 0.94, a severity-5 band** — but the score channel does register the injected content (F-041), so the picture is mixed and both reads must be co-reported; the hostile set is not a protected attribute, so its non-registration files under **reliability**, not protected-insensitivity. Locked the decision in a detailed amendment with exact remediation steps (analyzer fix + a PR to the L2 YAML prose), with **implementation deferred** to a fresh session on account of context load — no number has entered `findings.md`, so nothing incorrect propagates ([`fa0b788`](https://github.com/Eticas-AI/wiselook-audit/commit/fa0b788)). Separately: pre-registered the Strand-2 lexical/semantic homogenization analysis and reconciled the s25 handover notes (lexical leads downgraded, gender-guess dropped). Detail in [`meta/log/2026-07-02.md`](https://github.com/Eticas-AI/wiselook-audit/blob/main/meta/log/2026-07-02.md) (session 26).

> **Session-numbering note.** The 2026-06-26 Wiselook entries in W26 cite "(session 14)"; after the per-day-log migration the repo numbers that work **session 13** (`meta/log/2026-06-26.md`), and this close-out is **session 14** (`meta/log/2026-06-29.md`). The duplicate "session 14" label across the two work-log weeks is harmless — flagged here for reference. (The W26 entries also link "(session 14)" to `INTERNAL_LOG.md`; post-migration that detail lives in `meta/log/2026-06-26.md`.)

## DataPACT

- **2026-06-29: synced with Mariana to prepare the 30 Jun WP4 meeting and divide near-term Eticas tasks.** Briefed her on the outcomes of the Milano technical workshop; agreed she attends the WP4 meeting on 30 Jun with a short plan on Eticas's next steps. Task split: Module 1 application of the ethical & social impact framework to the use cases coordinated by Pablo (Jul–Sep, with Usman); Training and the Ethics Advisory Board coordinated by Mariana with Gemma and Alex, the EAB sequenced after the Module 1 application.
- **2026-06-29: progressed the framework-access API / Keycloak integration with ASSIST.** Following Milano, ASSIST (Dumitru Cenusa) offered integration support; proposed a working meeting on 14 Jul to define requirements. Pablo coordinates these technical developments as Eticas's point of contact.

## AI Risk Taxonomy

- **2026-07-02: verified v2.0.0 figures for Gemma's taxonomy paper, converted the paper draft to LaTeX, and packaged it for arXiv submission.** Verified taxonomy counts against a clean clone at the `v2.0.0` tag (10 categories / 20 sub-groups / 76 active subcategories / 18 frameworks, established-vs-emerging split for each), cross-checking the public-view subset separately via `build/generate_pages.py`'s `is_visible()` logic. Converted Gemma's Word draft of the accompanying paper to LaTeX, regenerating the 6 figures/tables as vector PDFs from the original SVG sources (trimmed the embedded "Table N." title from the 5 table SVGs to avoid doubling with LaTeX's auto-numbered caption). Compiled clean and pushed as `paper/` to `ai-risk-taxonomy` (`main`), holding back the original `.docx`. Submission is pending arXiv endorsement in `cs.CY` — Pablo requesting from Usman. Detail in [`ai-risk-taxonomy/meta/INTERNAL_LOG.md`](https://github.com/Eticas-AI/ai-risk-taxonomy/blob/main/meta/INTERNAL_LOG.md) (2026-07-02 entry).
